> **แทนที่แล้ว (12 ก.ย. 2569):** แหล่งอ้างอิงงานออกแบบย้ายไปที่
> `docs/design/social-tournament/` ชุดนี้เก็บไว้เพื่อตามรอย ไม่ได้ลบ
> token และโลโก้ของสองชุดเหมือนกัน ชุดใหม่เพิ่ม mascot แบบเวกเตอร์และชุด lockup

# ที่มาของชุดนี้

`longsanam-direction-04.zip` — Human in Motion · เกรพสร้างในระบบอื่นแล้วส่งเข้ามา 10 ก.ย. 2569

## สิ่งที่รับมาใช้จริงในแอป
- **โลโก้ A / Flow Runner** (ตัวเลือกที่ชุดนี้ปักว่าแนะนำ) → `src/components/brand-mark.tsx`
- **ไอคอนแอป** จาก `logo/app-circle.svg` → `src/app/icon.svg`
- **ภาษาภาพมุมโค้งสลับมุมตัด** → `--radius-button` / `--radius-card` ใน `src/app/globals.css`

## สิ่งที่ไม่ได้คัดลอกเข้ามา
- `fonts/` (3 MB TTF) — แอปโหลด Anuphan และ Bai Jamjuree ผ่าน `next/font/google` อยู่แล้ว
- `logo/lockup-a.svg` (107 KB) — ฝังฟอนต์เป็น base64 ในแอปใช้ตัวอักษรจริงคู่กับตราแทน
- `design-review.html` + `visual-language.css` — เป็นหน้ารีวิวพร้อม layout สาธิต ไม่ใช่โค้ดโปรดักชัน

## ตรวจแล้วก่อนรับเข้า
- คอนทราสต์ทั้ง 20 คู่ใน `system/contrast.json` คำนวณใหม่ด้วยสูตรอิสระ **ตรงทุกคู่**
  และไม่มีคู่ไหนอ้างว่าผ่านทั้งที่ไม่ผ่าน
- ทุกสีใน `system/tokens.json` **ตรงกับ `globals.css` เดิมทุกค่า** จึงไม่ต้องแก้ palette

## ข้อจำกัดที่ชุดนี้ประกาศเอง
มาสคอตเป็น **raster PNG** ไม่ใช่ SVG แยกท่า และไม่ใช่หุ่นข้อต่อ
ใช้เป็นทิศทางภาพประกอบได้ แต่ถ้าจะเอาลงโปรดักชันต้องวาด vector ใหม่ตามภาพ
และตรวจ anatomy กับอุปกรณ์อีกรอบ — ยังไม่ได้เอาเข้าแอป
