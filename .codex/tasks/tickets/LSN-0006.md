# LSN-0006 — Domain core: state machines and pure logic

**Type:** Feature · **Phase:** 2 · **Status:** Done

## Acceptance criteria
- [x] Session / participant / booking state machines with explicit legal transitions.
- [x] `evaluateBookingEligibility` — paid headcount and paid total vs thresholds.
- [x] `selectFallbackCourt` — priority order, approved-only, skips exhausted attempts.
- [x] `calculateRefund` — policy tiers by hours before start.
- [x] `planWaitlistPromotion` — first eligible entry, configurable payment window.
- [x] Unit tests cover all four plus the state machines.
