# LSN-0001 — Project scaffold, tooling, environment template

**Type:** Chore · **Phase:** 1 · **Status:** Done

## Goal
Stand up the Next.js + TypeScript + Tailwind + Supabase toolchain with tests wired.

## Acceptance criteria
- [x] Next.js (App Router) + TypeScript + Tailwind scaffolded under `src/`.
- [x] `@supabase/supabase-js` + `@supabase/ssr` installed.
- [x] Vitest configured with `@/*` path alias; `npm test` runs.
- [x] `supabase init` complete; local stack starts with `supabase start`.
- [x] `.env.example` documents every variable the app reads.
- [x] `CLAUDE.md` records project conventions.
