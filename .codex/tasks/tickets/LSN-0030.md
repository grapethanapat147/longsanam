# 🟢 LSN-0030 — บันทึกผลแมตช์ + ยืนยันสองฝั่ง

**Type:** Feature · **Phase:** 10 · **Status:** Review
**Spec:** `.codex/specs/tournaments.md` หัวข้อ "เรื่องที่อันตรายที่สุด: คะแนนฝีมือ"
**Depends on:** LSN-0029 — **merge เข้า main แล้ว 17 ก.ย. 2569**

## Goal

บันทึกผลการแข่งรายแมตช์ในทัวร์นาเมนต์ และ **ผลจะนับได้ก็ต่อเมื่ออีกฝั่งยืนยัน**

ตั๋วนี้ไม่ได้คิดคะแนนอะไรทั้งนั้น มันสร้าง **แหล่งข้อมูลที่โกงยาก** ให้ LSN-0031
ใช้คิดคะแนนฝีมือทีหลัง

## Context

`.codex/specs/tournaments.md` ตัดสินไว้ว่าคะแนนที่กำหนดรุ่นต้องมาจาก
**ผลการแข่ง ไม่ใช่ความเห็น** เหตุผลคือทัวร์นาเมนต์ผลิตข้อมูลที่โกงยากอยู่แล้ว
คือ *ใครชนะใคร* — การทำให้คะแนนตัวเองต่ำแปลว่าต้องแพ้จริง ซึ่งมีราคาที่ต้องจ่าย
ต่างจากการกดตัวเลขในฟอร์ม

**แต่ข้อมูลนั้นจะโกงยากก็ต่อเมื่อฝั่งเดียวกรอกเองไม่ได้** สเปกเขียนไว้ตรง ๆ ว่า

> ผลแมตช์ที่ฝั่งเดียวกรอกได้เองคือช่องโหว่เดียวกับการให้คะแนนกันเอง
> ต้องให้อีกฝั่งยืนยันก่อนจึงนับเข้าคะแนน

**การยืนยันสองฝั่งจึงเป็นหัวใจของตั๋วนี้ ไม่ใช่ของแถม** ถ้าข้อนี้พัง
คะแนนฝีมือทั้งระบบใน LSN-0031 ก็ไร้ความหมายตั้งแต่วันแรก

ของที่พึ่งมีครบแล้ว — `tournaments`, `tournament_teams`, `groups`,
`group_members` และกลไก `app_log()`

## Scope

- ตาราง `matches` พร้อมรายชื่อผู้เล่นสองฝั่งและสกอร์
- บันทึกผล · ยืนยันโดยอีกฝั่ง · โต้แย้งผล
- สถานะแมตช์: `recorded → confirmed` และ `recorded → disputed`
- หน้าจอบันทึกผลและยืนยันผล ฝั่งเจ้าภาพและฝั่งทีม
- นับเฉพาะแมตช์ที่ `confirmed` เท่านั้นในทุกที่ที่อ่านผล

## Non-goals

- **ไม่คิดคะแนนฝีมือ ไม่มีตาราง `player_skill`** — LSN-0031
- ไม่ทำ bracket หรือจัดตารางแข่งอัตโนมัติ — spec ระบุเป็น non-goal
- ไม่ทำสถิติรายแต้ม ไม่ถ่ายทอดสด
- ไม่ตัดสินอันดับสุดท้ายของทัวร์นาเมนต์ (แชมป์) — ดู Open Questions ข้อ 3
- ไม่แตะ `player_credit` และไม่แตะ `payments` ไม่ว่าทางใด

## Decisions — มาจาก spec แล้ว ผูกพันตั๋วนี้

### 1. ผลนับได้ต่อเมื่ออีกฝั่งยืนยัน

แมตช์ที่เพิ่งบันทึกอยู่สถานะ `recorded` ซึ่ง **ยังไม่นับเป็นผลการแข่ง**
ต้องมีคนจากทีมตรงข้ามกดยืนยันจึงกลายเป็น `confirmed`

### 2. คนที่บันทึกยืนยันเองไม่ได้ และคนในทีมเดียวกันก็ยืนยันแทนไม่ได้

ถ้าคนในทีมเดียวกันยืนยันได้ การยืนยันก็ไม่ได้แปลว่าอะไรเลย
ต้องเป็นสมาชิกของ **ก๊วนฝั่งตรงข้ามในแมตช์นั้น**

### 3. โต้แย้งได้ และผลที่ถูกโต้แย้งไม่นับ

สเปกเขียนว่า *"กรณีขัดแย้งให้รอผู้จัดตรวจ ยังไม่ส่งผลต่อคะแนน"*
สถานะ `disputed` จึงมีค่าเท่ากับ `recorded` ในแง่การนับ คือ **ไม่นับ**

### 4. เก็บรายชื่อผู้เล่นสองฝั่ง ไม่ใช่แค่ชื่อก๊วน

Decision 3 ของสเปกบอกว่าการตรวจรุ่นต้องตรวจกับ **รายชื่อที่ลงเล่นจริง
ไม่ใช่ค่าเฉลี่ยของทั้งก๊วน** เพราะก๊วน 12 คนที่ส่งมือดีสุด 4 คนลงรุ่นอ่อน
คือการโกงแบบเดียวกับที่ระบบพยายามกัน

→ `matches` ต้องเก็บ **ว่าใครลงเล่นจริงในแมตช์นั้น** ไม่งั้น LSN-0031
จะไม่มีข้อมูลให้ตรวจ

## Design

```sql
create type public.match_status as enum ('recorded', 'confirmed', 'disputed', 'voided');

create table public.matches (
  id              uuid primary key default gen_random_uuid(),
  tournament_id   uuid not null references public.tournaments(id) on delete cascade,
  side_a_group_id uuid not null references public.groups(id),
  side_b_group_id uuid not null references public.groups(id),
  side_a_players  uuid[] not null,
  side_b_players  uuid[] not null,
  score_a         integer not null check (score_a >= 0),
  score_b         integer not null check (score_b >= 0),
  court_label     text,                      -- ป้ายสำหรับคนอ่าน ไม่มีอะไรคำนวณจากมัน
  played_at       timestamptz not null default now(),
  status          public.match_status not null default 'recorded',
  recorded_by     uuid not null references public.profiles(id),
  confirmed_by    uuid references public.profiles(id),
  confirmed_at    timestamptz,
  disputed_by     uuid references public.profiles(id),
  disputed_at     timestamptz,
  dispute_note    text,
  voided_by       uuid references public.profiles(id),
  voided_at       timestamptz,
  void_reason     text,
  created_at      timestamptz not null default now(),
  check (side_a_group_id <> side_b_group_id),
  check (array_length(side_a_players, 1) between 1 and 2),
  check (array_length(side_b_players, 1) between 1 and 2),
  -- ยืนยันแล้วต้องมีคนยืนยัน และคนยืนยันต้องไม่ใช่คนบันทึก
  check (status <> 'confirmed' or (confirmed_by is not null and confirmed_by <> recorded_by))
);
```

**`check (confirmed_by <> recorded_by)` ที่ระดับตาราง ไม่ใช่แค่ใน RPC**
เพราะกติกาข้อนี้คือทั้งหมดของความน่าเชื่อถือของคะแนนในตั๋วถัดไป
ด่านที่อยู่ในโค้ดอย่างเดียวคือด่านที่หายไปได้ตอนมีคนเขียน RPC ตัวที่สอง

## Acceptance Criteria

- [x] บันทึกผลได้เฉพาะสมาชิกของก๊วนที่อยู่ในแมตช์นั้น หรือเจ้าภาพ — ปักด้วยเทส
- [x] แมตช์ที่เพิ่งบันทึกมีสถานะ `recorded` **ยังไม่นับเป็นผล** — ปักด้วยเทส
- [x] **คนที่บันทึกยืนยันเองไม่ได้** — ปักด้วยเทส
- [x] **คนในก๊วนเดียวกับผู้บันทึกยืนยันแทนไม่ได้** — ปักด้วยเทส
- [x] สมาชิกก๊วนฝั่งตรงข้ามยืนยันได้ แล้วสถานะเป็น `confirmed` — ปักด้วยเทส
- [x] **ฐานข้อมูลปฏิเสธแถวที่ `confirmed` แต่ `confirmed_by = recorded_by`**
      แม้จะ insert ตรง ๆ ไม่ผ่าน RPC — ปักด้วย `throws_ok` ที่อ้างชื่อ constraint
- [x] โต้แย้งผลได้ และแมตช์ที่ `disputed` ไม่ถูกนับเหมือน `recorded` — ปักด้วยเทส
- [x] **เจ้าภาพ `void` ผลที่ยืนยันแล้วได้ ทีมใดทีมหนึ่ง void ไม่ได้** — ปักด้วยเทส
- [x] **ตัวเลขสกอร์ของแถวที่ `confirmed` แก้ไม่ได้** ต้อง void แล้วบันทึกใหม่ — ปักด้วยเทส
- [x] **มีสถานะเดียวที่นับคือ `confirmed`** — `recorded` `disputed` `voided` ไม่นับ — ปักด้วยเทส
- [x] ยืนยันซ้ำหรือโต้แย้งซ้ำ ได้ผลเท่าเดิม ไม่สร้างแถวหรือเปลี่ยนเวลาเดิม — ปักด้วยเทส
- [x] บันทึกแมตช์ในทัวร์นาเมนต์ที่ยังไม่ `ready` หรือถูกยกเลิก ถูกปฏิเสธ — ปักด้วยเทส
      **allowlist ไม่ใช่ denylist** ตามบทเรียน LSN-0020 และ LSN-0029
- [x] ผู้เล่นที่ระบุในแต่ละฝั่งต้องเป็นสมาชิกของก๊วนฝั่งนั้นจริง — ปักด้วยเทส
- [x] คนนอกทัวร์นาเมนต์อ่าน `matches` ตรง ๆ ไม่ได้ — ปักด้วย pgTAP
- [x] **`player_credit` ไม่ขยับเลย และไม่มี `alter table public.payments`** — ปักด้วยเทส
- [x] pgTAP ของ LSN-0019 ถึง LSN-0029 ยังเขียวโดยไม่ต้องแก้เทส
- [x] ทุกการเปลี่ยนสถานะเขียน `audit_logs` ผ่าน `app_log()`
- [x] ไม่มี UI ที่กดแล้วไม่ทำอะไร

## Validation

- `npm run lint`, `npx tsc --noEmit`, `npm test` ผ่าน
- `npm run test:db` ผ่าน รวมข้อใหม่ และของเดิมไม่แดง
- migration apply จาก `npm run db:reset` ได้
- ตรวจบนเบราว์เซอร์ 375×812 สามมุมมอง: คนบันทึกผล · คนฝั่งตรงข้ามที่ต้องยืนยัน ·
  คนในทีมเดียวกับผู้บันทึก (ต้องไม่เห็นปุ่มยืนยัน)
- seed มีแมตช์ตัวอย่างทั้งที่ `recorded` รอยืนยัน และที่ `confirmed` แล้ว

## Open Questions — ตัดสินครบแล้ว 17 ก.ย. 2569

### 1. แก้ผลที่ยืนยันแล้ว → **แก้ไม่ได้ แต่เจ้าภาพ `void` ได้ แล้วบันทึกใหม่**

เพิ่มสถานะ `voided` ผลที่ยืนยันแล้วจะไม่ถูกแก้ตัวเลขในแถวเดิมเด็ดขาด
ถ้าผิดจริง เจ้าภาพกด void พร้อมเหตุผล แล้วบันทึกแมตช์ใหม่ให้ถูก
แล้วให้อีกฝั่งยืนยันใหม่ตามปกติ

**เหตุผลที่ไม่เลือก "แก้ไม่ได้เลย"** ผลที่ผิดและซ่อมไม่ได้จะปนเปื้อนคะแนนฝีมือ
ถาวร ซึ่งแย่กว่าความยุ่งยากของการมีทางออก

**เหตุผลที่ไม่เลือก "แก้ตัวเลขในแถวเดิม"** ถ้าแถวที่ยืนยันแล้วเปลี่ยนค่าได้
LSN-0031 ต้องมีกลไกคิดคะแนนย้อนหลัง ซึ่งเป็นความซับซ้อนคนละระดับ
แต่ถ้าใช้ void แทน **ชุดของแมตช์ที่ `confirmed` แค่เล็กลง** LSN-0031
จึงคิดคะแนนใหม่จากชุดปัจจุบันได้เหมือนเดิม ไม่ต้องรู้ประวัติ

**เฉพาะเจ้าภาพ void ได้ ไม่ใช่ทีมใดทีมหนึ่ง** เพราะทีมที่ void ผลที่ตัวเองแพ้ได้
คือช่องโกงตรงตัว และสเปกวางเจ้าภาพเป็นคนกลางไว้แล้วในเส้นทางข้อโต้แย้ง
(*"กรณีขัดแย้งให้รอผู้จัดตรวจ"*)

**`voided` ไม่นับเหมือน `recorded` และ `disputed`** — มีสถานะเดียวที่นับคือ `confirmed`

### 2. `matches.court_id` → **ตัดทิ้ง ใช้ `court_label text` ที่ไม่บังคับแทน**

LSN-0029 ตัดสินให้เจ้าภาพยืนยันคอร์ตเองด้วย `venue_note` ที่เป็นข้อความ
ระบบจึง **ไม่มีแถวคอร์ตให้ผูก** การใส่ `court_id` ที่ชี้ไป `courts` จะเป็นคอลัมน์
ที่ว่างตลอดไปสำหรับทัวร์นาเมนต์ทุกงานที่ไม่ได้ใช้สนามพันธมิตร ซึ่งคือทุกงานวันนี้

เก็บเป็นข้อความเพราะวันแข่งจริงที่มีหลายคอร์ตวิ่งพร้อมกัน คนเรียกแมตช์กันว่า
"คอร์ต 3" และนั่นคือสิ่งที่ต้องบันทึก **ไม่มีอะไรในระบบอ่านค่านี้ไปคำนวณ**
มันเป็นป้ายสำหรับคนอ่านอย่างเดียว และเขียนไว้ตรงนี้ให้ชัดว่าตั้งใจให้เป็นแบบนั้น

### 3. ใครตัดสินแชมป์ → **เจ้าภาพยืนยันอันดับ แต่ไม่ทำในตั๋วนี้**

**คำตอบของ "ใคร" คือเจ้าภาพ** ซึ่งตรงกับที่ `badge-definitions.json` เขียนไว้แล้ว
ว่า *"ให้หลังผู้จัดยืนยันอันดับสุดท้าย"* ระบบคำนวณเองไม่ได้ เพราะสเปกระบุว่า
**ไม่ทำ bracket และไม่จัดตารางแข่ง** เมื่อไม่มีรูปแบบการแข่งที่ระบบรู้
คำว่า "ใครชนะทั้งรายการ" ก็คำนวณไม่ได้ตามนิยาม

**แต่มันไม่ควรอยู่ในตั๋วนี้** เพราะตั๋วนี้ทั้งใบตั้งอยู่บนหลักว่า
**ไม่มีฝ่ายไหนตัดสินผลได้ฝ่ายเดียว** ส่วนการประกาศแชมป์เป็นการกระทำที่
**เจ้าภาพทำฝ่ายเดียวโดยตั้งใจ** เอาสองอย่างมาไว้ในตั๋วเดียวกันทำให้สารของตั๋วขุ่น
และทำให้คนอ่านโค้ดทีหลังเข้าใจผิดว่าการยืนยันสองฝั่งเป็นทางเลือก

→ **ที่เก็บอันดับสุดท้ายและ RPC ประกาศแชมป์ ย้ายไปอยู่ใน LSN-0028 (Badge)**
ซึ่งเป็นผู้ใช้เพียงรายเดียวของข้อมูลนั้น และเป็นตั๋วที่บล็อกอยู่ด้วยเรื่องนี้พอดี

**ข้อควรระวังที่ต้องยกไปกับมัน:** เจ้าภาพประกาศก๊วนตัวเองเป็นแชมป์ได้
ต้องมีอย่างน้อยหนึ่งแมตช์ที่ `confirmed` ของทีมนั้น และการประกาศต้องเขียน
`audit_logs` และมองเห็นได้จากทุกทีม ไม่ใช่ทำเงียบ ๆ

## Implementation plan

**วันที่วางแผน:** 17 ก.ย. 2569 · **สาขา:** `feature/LSN-0030`

### Task 1 — migration: ตาราง สิทธิ์ และ constraint ที่เป็นหัวใจ

`supabase/migrations/20260920000100_matches.sql`

- `match_status` enum · ตาราง `matches` ตาม Design
- **`check (status <> 'confirmed' or confirmed_by <> recorded_by)`** ที่ระดับตาราง
  ตั้งชื่อ constraint ให้ชัด เพราะเทสจะอ้างชื่อมันตรง ๆ
- `grant all privileges ... to service_role`
- **ห้ามมี `alter table public.payments` และห้ามแตะ `player_credit`**

### Task 2 — RLS + helper

- ใช้ `is_tournament_host` / `is_tournament_team_member` ที่มีอยู่แล้วจาก LSN-0029
  ไม่เขียนใหม่
- `matches` อ่านได้เฉพาะคนที่เกี่ยวข้องกับทัวร์นาเมนต์นั้น เขียนผ่าน RPC เท่านั้น

### Task 3 — RPC

| ฟังก์ชัน | กติกา |
| --- | --- |
| `record_match(...)` | เฉพาะสมาชิกก๊วนที่อยู่ในแมตช์ หรือเจ้าภาพ · **allowlist สถานะทัวร์นาเมนต์** (`ready`, `booked`, `completed`) · ผู้เล่นแต่ละฝั่งต้องเป็นสมาชิกก๊วนฝั่งนั้นจริง |
| `confirm_match(id)` | **เฉพาะสมาชิกก๊วนฝั่งตรงข้าม** และต้องไม่ใช่ `recorded_by` |
| `dispute_match(id, note)` | ฝั่งตรงข้ามโต้แย้งได้ · `disputed` ไม่นับเหมือน `recorded` |
| `void_match(id, reason)` | **เฉพาะเจ้าภาพ** · ใช้ได้กับแมตช์ที่ `confirmed` แล้วด้วย |

ทุกตัวเขียน `app_log()` — helper ชื่อ `app_log` ไม่ใช่ตาราง `audit_logs`

**สิ่งที่ต้องระวังเป็นพิเศษ:** `confirm_match` ต้องหาว่าผู้เรียกอยู่ฝั่งไหน
แล้วอนุญาตเฉพาะฝั่งที่ **ไม่ใช่** ฝั่งของ `recorded_by` — ไม่ใช่แค่เช็คว่า
`auth.uid() <> recorded_by` ซึ่งจะปล่อยให้เพื่อนร่วมก๊วนยืนยันแทนได้

### Task 4 — pgTAP `supabase/tests/matches.test.sql`

ปักทุก AC โดยเฉพาะห้าข้อที่เป็นหัวใจ

- คนบันทึกยืนยันเองไม่ได้
- **คนในก๊วนเดียวกับผู้บันทึกยืนยันแทนไม่ได้** ← ข้อที่พลาดง่ายที่สุด
- `throws_ok` อ้าง **ชื่อ constraint** เมื่อ insert แถว `confirmed` ที่
  `confirmed_by = recorded_by` ตรง ๆ ไม่ผ่าน RPC
- เจ้าภาพ void ได้ · ทีมใดทีมหนึ่ง void ไม่ได้
- **มีสถานะเดียวที่นับคือ `confirmed`** — เขียนเป็น helper
  `confirmed_matches(tournament_id)` แล้วปักว่ามันไม่คืน `recorded`/`disputed`/`voided`

### Task 5 — seed · Task 6 — TypeScript · Task 7 — UI

- seed: แมตช์สองแถว หนึ่ง `recorded` รอยืนยัน หนึ่ง `confirmed` แล้ว
- `src/lib/actions/match.ts` · regen `src/types/database.ts`
- หน้าแมตช์ในหน้าทัวร์นาเมนต์: รายการแมตช์ · ฟอร์มบันทึกผล · ปุ่มยืนยัน/โต้แย้ง
- **ปุ่มยืนยันต้องไม่ขึ้นเลยสำหรับคนฝั่งเดียวกับผู้บันทึก** ไม่ใช่ขึ้นแล้วกดไม่ได้

### Task 8 — ตรวจของจริง

`db:reset` · `test:db` · `verify` · `build` แล้วตรวจเบราว์เซอร์ 375×812
สามมุมมองตาม Validation

### ความเสี่ยงที่รู้ตัวอยู่

| ความเสี่ยง | กัน |
| --- | --- |
| **เพื่อนร่วมก๊วนยืนยันแทนได้** | ตรวจจาก *ฝั่ง* ไม่ใช่จาก user id + เทสปักไว้ |
| ด่านยืนยันอยู่แค่ใน RPC | `check` ที่ระดับตารางด้วย |
| บันทึกแมตช์ในงานที่ยังไม่พร้อม | allowlist สถานะ ตามบทเรียน LSN-0020 และ LSN-0029 |
| ใส่ผู้เล่นที่ไม่ได้อยู่ก๊วนนั้น | ตรวจกับ `group_members` ใน RPC + เทส |
| นับแมตช์ที่ยังไม่ยืนยัน | helper ตัวเดียวที่ทุกที่เรียก ไม่ให้แต่ละที่กรองเอง |

---

## Blocking notes

- **LSN-0031 (คะแนนฝีมือ) พึ่งตั๋วนี้ทั้งหมด** — ไม่มีผลแมตช์ที่ยืนยันแล้ว
  ก็ไม่มีอะไรให้คิดคะแนน
- **LSN-0028 (Badge) ปลดบล็อกได้บางส่วนหลังตั๋วนี้** — ใบ `02-new-opponents`
  และ `06-three-squads` นับจาก "แมตช์ที่ยืนยันผลแล้ว" ซึ่งตั๋วนี้ให้ได้
  ส่วนใบ `04-champion` ยังติด Open Questions ข้อ 3
- ไม่ทับกับตั๋วอื่นที่เปิดอยู่

---

## Review Notes — 17 ก.ย. 2569

### Gate

| ด่าน | ผล |
| --- | --- |
| `npm run db:reset` | ผ่าน migration ใหม่ apply ได้ |
| `npm run test:db` | **185 เทส ผ่านหมด** (เดิม 161 + ของตั๋วนี้ 24) |
| `npm run lint` · `npx tsc --noEmit` · `npm test` | ผ่าน 143 เทส |
| `npm run build` | ผ่าน |
| เบราว์เซอร์ 375×812 สามมุมมอง | ผ่านครบ ดูตาราง "ตรวจของจริง" ข้างล่าง |

pgTAP ของ LSN-0019 ถึง LSN-0029 เขียวโดยไม่ต้องแก้ไฟล์เทสเดิมแม้แต่บรรทัดเดียว —
ไฟล์เทสที่แตะในตั๋วนี้มีแค่ `supabase/tests/matches.test.sql` ซึ่งเป็นไฟล์ใหม่

### ตรวจของจริงบนเบราว์เซอร์ — สามมุมมองตาม Validation

| มุมมอง | ใคร | เห็นอะไร |
| --- | --- | --- |
| คนบันทึกผล | ก้อง (เจ้าภาพ ผู้บันทึก) | ไม่มีปุ่มยืนยัน เห็นเหตุผลแทน |
| ฝั่งตรงข้าม | มีน (แบดเช้าพระราม 9) | เห็น "ยืนยันผล" + "โต้แย้งผล" **กดแล้วสถานะเปลี่ยนเป็น ยืนยันแล้ว จริง** |
| เพื่อนร่วมก๊วนผู้บันทึก | บอส (ก๊วนเดียวกับก้อง) | ไม่มีปุ่ม เห็น "รอฝั่งตรงข้ามยืนยัน — คุณยืนยันผลที่ฝั่งตัวเองบันทึกก็ไม่ได้" |

### บั๊กที่เจอระหว่างตรวจ แล้วแก้ในตั๋วนี้

**ฝั่งตรงข้ามไม่เห็นปุ่มยืนยัน** — policy `group_members_read` เดิมให้เห็นเฉพาะ
สมาชิกก๊วนตัวเอง หน้าจอจึงคำนวณ `canConfirm` จากรายชื่อที่ไม่ครบ และฟอร์มบันทึกผล
ก็เลือกผู้เล่นฝั่งตรงข้ามไม่ได้ด้วย แก้โดยเพิ่ม `shares_tournament_with_group()`
เข้า policy เจอเพราะตรวจบนจอจริง ไม่ใช่จากการอ่านโค้ด — เทส pgTAP ทุกข้อเขียวอยู่แล้ว
ตอนที่บั๊กนี้ยังอยู่ เพราะ pgTAP เรียก RPC ตรง ๆ ไม่ผ่านชั้นที่อ่าน `group_members`

### สองข้อที่ยังไม่ได้ปักตอนขอ move-review แล้วเพิ่มให้ครบ

ตอนไล่ Acceptance Criteria ทีละข้อพบว่าสองข้อเขียนว่า "ปักด้วยเทส" แต่ไม่มีเทสจริง
จึงเพิ่มก่อนย้ายเข้า Review (22 → 24 เทส)

1. **สกอร์ของแถวที่ `confirmed` แก้ไม่ได้** — ด่านจริงไม่ใช่ constraint แต่คือ
   `revoke insert, update, delete ... from authenticated` บวกกับที่ `matches`
   มีแต่ policy ของ `select` ปักเป็น errcode `42501` ไม่ใช่ "update แล้วได้ 0 แถว"
   เพราะถ้ามีคนเพิ่ม policy ของ update เข้ามาวันหลัง เทสแบบนับแถวจะยังเขียว
2. **โต้แย้งซ้ำได้ผลเท่าเดิม** — เดิมปักแต่ `confirm` ซ้ำ ไม่ได้ปัก `dispute` ซ้ำ

**Falsify แล้วทั้งคู่** — `grant update on matches to authenticated` +
ถอด early-return ของ `replayed` ใน `dispute_match` ออก แล้วรันใหม่ ผลคือ
`Failed tests: 13, 17` พอดีสองข้อนั้น ไม่มีข้ออื่นแดงตาม แล้ว `db:reset` คืนสภาพ

### Definition of Done ข้อ LSN-0024 — การผ่อนคลาย invariant

ตั๋วนี้ **ผ่อนคลาย policy `group_members_read`** ให้กว้างขึ้น (จาก "ก๊วนตัวเอง"
เป็น "ก๊วนตัวเอง หรือก๊วนที่ร่วมงานเดียวกัน") จึงต้องไล่ผู้อ่านเดิมของ invariant เก่า

หาโดย `grep -rn "from('group_members')" src/` สำหรับฝั่ง TypeScript และ
query `pg_proc` ตรง ๆ สำหรับฝั่ง SQL (`prosrc like '%group_members%'`) แทนการ grep
ไฟล์ migration เพราะฟังก์ชันถูก `create or replace` ข้ามหลาย migration การ grep
ไฟล์จะนับตัวที่ถูกเขียนทับไปแล้วด้วย

**เจอผู้อ่าน 18 จุด — SQL 12 ฟังก์ชัน และ TypeScript 6 call site**

SQL ทั้ง 12 ฟังก์ชันเป็น `security definer` ทุกตัว (ตรวจจาก `prosecdef` ไม่ใช่จาก
การอ่านโค้ด) จึงไม่ได้อ่านผ่าน policy นี้ตั้งแต่แรกและไม่กระทบเลย

ฝั่ง TypeScript 6 จุด ไล่ทีละจุดว่าผลลัพธ์เปลี่ยนไหม

| จุด | กรอง | ผลหลังผ่อนคลาย |
| --- | --- | --- |
| `app/groups/page.tsx:17` | `user_id = ตัวเอง` | ไม่เปลี่ยน |
| `app/groups/[id]/page.tsx:44` | `group_id` ไม่กรองคน | **เข้าไม่ถึง** — `groups_read` ยังบังคับ `is_group_member` อยู่ หน้าจึงตอบ "ไม่พบหรือไม่ใช่สมาชิก" ก่อนถึงคิวรีนี้ |
| `app/tournaments/[id]/page.tsx:55` | `user_id = ตัวเอง` | ไม่เปลี่ยน |
| `app/tournaments/[id]/page.tsx:73` | `group_id in (ทีมในงาน)` | **นี่คือจุดที่ตั้งใจให้กว้างขึ้น** |
| `t/[code]/page.tsx:47` | `user_id = ตัวเอง` | ไม่เปลี่ยน |
| `lib/actions/session.ts:124` | `group_id` + `user_id = ตัวเอง` | ไม่เปลี่ยน — จุดนี้ใช้เป็น **ด่านตรวจสมาชิก** ถ้าพังคือคนนอกตั้งนัดในนามก๊วนคนอื่นได้ แต่ policy ที่กว้างขึ้นเพิ่มแถว *ของคนอื่น* ในก๊วนที่ร่วมงานกัน ไม่ได้เพิ่มแถวที่ `user_id` เป็นตัวเองในก๊วนที่ตัวเองไม่ได้อยู่ ด่านจึงยังอยู่ครบ |

สรุป มีจุดเดียวที่ผลลัพธ์เปลี่ยนจริง และเป็นจุดที่ตั้งใจแก้

สิ่งที่หลุดออกไปคือ **รายชื่อสมาชิกก๊วนคู่แข่งในงานเดียวกัน** ซึ่งเป็นข้อมูลที่
จำเป็นต้องเห็นอยู่แล้วเพื่อเลือกผู้เล่นลงแมตช์ ไม่ใช่ข้อมูลการเงินหรือเบอร์ติดต่อ

### เรื่องที่พบระหว่างตรวจ แต่ **ไม่ได้แก้ในตั๋วนี้** — เป็นของ LSN-0029

`src/app/app/tournaments/[id]/page.tsx:45` นับ "ทุกทีมจ่ายแล้ว x/y" จากแถวที่
RLS ยอมให้เห็น ไม่ใช่จำนวนจริง policy `tournament_payments_read` ให้เห็นเฉพาะ
ก๊วนตัวเองกับเจ้าภาพ สมาชิกทีมที่ไม่ใช่เจ้าภาพจึงเห็น "1/2 ยังไม่ครบ" ตลอด
และเห็นทีมคู่แข่งที่จ่ายแล้วขึ้นป้าย "ยังไม่จ่าย"

พิสูจน์ด้วยการสวมบทบาท: `postgres` เห็น 2 แถว · บอส (สมาชิกก๊วน 1) เห็น 1 แถว

ขัดกับคำสัญญาหลักของตัวสินค้าโดยตรง ทางแก้คืออ่านตัวเลขประตูจาก RPC
`evaluate_tournament_gates` ที่มี authorization check อยู่แล้ว แทนการนับแถวดิบ
**ยังไม่เปิดตั๋ว** — รอเกรพตัดสินว่าจะแยกตั๋วบั๊กหรือแก้พ่วง

---

## Review — 17 ก.ย. 2569 · **NEEDS FIX → แก้แล้ว → PASS**

ใช้ `scrutinize` เพราะตั๋วนี้แก้ RLS policy ที่มีอยู่แล้ว เพิ่มตารางใหม่พร้อม
`security definer` สี่ตัว และเป็นฐานของคะแนนฝีมือใน LSN-0031 ทั้งหมด
รีวิวแบบคนนอก ไม่เชื่อคำอธิบายในคอมเมนต์ของตัวเอง แล้ว**ลองยิงจริงทุกข้อที่สงสัย**
แทนการอ่านโค้ดแล้วสรุป

### สิ่งที่เจอ — สามข้อ แก้ครบแล้วในตั๋วนี้

**1. `matches_side_a_size` ไม่ได้กันอะไรเลยเมื่ออาเรย์ว่าง — blocker**

```sql
check (array_length(side_a_players, 1) between 1 and 2)
```

`array_length('{}'::uuid[], 1)` คืน **NULL** ไม่ใช่ 0 และ CHECK ที่ได้ NULL คือ
CHECK ที่ **ผ่าน** (CHECK ตกก็ต่อเมื่อได้ FALSE) แถวที่ไม่มีผู้เล่นสักคนจึงลอด
ด่านที่หน้าตาเหมือนบังคับ 1–2 ไปได้เงียบ ๆ

ยิงจริงในฐานะก้อง: `record_match(... '{}'::uuid[] ...)` →
`{"ok": true, "status": "recorded"}` ทั้งที่ `src/lib/actions/match.ts:59`
กันอาเรย์ว่างไว้แล้ว — แต่เป็นด่านฝั่ง **หน้าจอ** และ `record_match` ถูก
`grant execute ... to authenticated` ใครถือ anon key ก็เรียกตรงได้

แก้เป็น `coalesce(array_length(...), 0) between 1 and 2` ทั้งสองฝั่ง

**2. คนเดียวกันอยู่ได้ทั้งสองฝั่งของแมตช์เดียวกัน — blocker**

LSN-0026 ตัดสินว่าคนหนึ่งอยู่ได้หลายก๊วน (แนนใน seed อยู่ทั้ง GROUP01 และ
GROUP02) จึงใส่ชื่อเธอทั้ง `side_a_players` และ `side_b_players` ได้ ยิงจริง
แล้วได้ `{"ok": true, "status": "recorded"}` แปลว่าเธอแข่งกับตัวเอง และ
LSN-0031 จะบวกและลบคะแนนคนเดียวกันจากแมตช์เดียว ที่แย่กว่าคือแมตช์แบบนี้
`confirmed` ได้จริง เพราะคนที่ยืนยันเป็นสมาชิกอีกฝั่งที่ไม่ใช่แนน

แก้ด้วย constraint `matches_no_player_on_both_sides check (not (side_a_players && side_b_players))`
บวกด่านใน RPC ที่ให้เหตุผลอ่านรู้เรื่อง และกันชื่อซ้ำในฝั่งเดียวกันด้วย
(ข้อหลังอยู่ใน RPC อย่างเดียว เพราะ CHECK มี subquery ไม่ได้)

**3. `recorder_cannot_confirm` เป็นสาขาที่ไม่มีวันถูกเรียกถึง — minor**

เช็ค `v_user = v_m.recorded_by` เคยอยู่ **หลัง** การเทียบฝั่ง แต่ฝั่งของคนบันทึก
ย่อมเท่ากับฝั่งของตัวเองเสมอ `same_side_cannot_confirm` จึงตอบไปก่อนทุกครั้ง
หลักฐานอยู่ในเทสของตั๋วนี้เอง: เทส "คนที่บันทึกยืนยันเองไม่ได้" คาดหวัง
`same_side_cannot_confirm` ไม่ใช่ชื่อเหตุผลที่ตรงกับสิ่งที่เกิดขึ้น และ
`src/i18n/th.ts` ก็มีข้อความไทยของเหตุผลที่ไม่มีวันโผล่บนหน้าจอ

ย้ายเช็คขึ้นก่อนคำนวณฝั่ง ตอนนี้สามเหตุผลแยกกันชัดและถูกเรียกถึงได้ทุกอัน
(`recorder_cannot_confirm` · `same_side_cannot_confirm` · `in_both_sides_cannot_confirm`)

### เทสที่เพิ่มจากรีวิว — 24 → 29 (ทั้งโปรเจกต์ 190)

ห้าข้อ: RPC ปฏิเสธฝั่งว่าง · RPC ปฏิเสธคนอยู่สองฝั่ง · RPC ปฏิเสธชื่อซ้ำ ·
**ตารางปฏิเสธฝั่งว่างแม้ insert ตรง ๆ** · **ตารางปฏิเสธคนอยู่สองฝั่งแม้ insert ตรง ๆ**

สองข้อท้ายปักที่ระดับตารางด้วยเหตุผลเดียวกับ `matches_confirmer_is_not_recorder`
คือ RPC ตัวที่สองหรือ `service_role` เขียนตรงเข้าตารางได้ ด่านที่อยู่ในโค้ด
อย่างเดียวคือด่านที่หายไปได้เงียบ ๆ

**Falsify แล้วทั้งหมด** — คืน constraint เดิม ถอดด่านสามอันออกจาก `record_match`
และย้ายเช็คใน `confirm_match` กลับที่เดิม แล้วรันใหม่ ได้ `Failed tests: 3-7, 11`
พอดีหกข้อนั้น ไม่มีข้ออื่นแดงตาม แล้ว `db:reset` คืนสภาพ

### ที่ตรวจแล้วไม่พบปัญหา

- **`grant`/`revoke`** — `revoke insert, update, delete from anon, authenticated`
  บวกกับที่ `matches` มีแต่ policy ของ `select` ทางเดียวที่แก้ผลได้จึงเป็น RPC
- **allowlist สถานะทัวร์นาเมนต์** — `ready`/`booked`/`completed` เท่านั้น
- **ทั้งสองก๊วนต้องเป็นทีมในงานจริง** — ตรวจกับ `tournament_teams`
- **`app_log()`** ครบทั้งสี่การเปลี่ยนสถานะ
- **`player_credit` และ `payments` ไม่ถูกแตะ** — ปักด้วยเทส
- **เจ้าภาพที่ไม่ได้อยู่ทั้งสองก๊วน** บันทึกผลได้ แล้วฝั่งใดฝั่งหนึ่งยืนยันได้
  (`v_recorder_side = 'h'`) — ตั้งใจแบบนี้ ผู้บันทึกเป็นกลาง คนยืนยันจึงเป็น
  คู่กรณีจริงหนึ่งฝั่ง ไม่ใช่ช่องโหว่

### ข้อสังเกตฝากไว้ให้ LSN-0031 — ไม่ใช่บั๊กของตั๋วนี้

`confirmed_matches()` เป็น `stable` เฉย ๆ ไม่ใช่ `security definer` จึงคืนเฉพาะ
แถวที่ผู้เรียกมีสิทธิ์เห็น **วันนี้ไม่เป็นปัญหา** เพราะ `matches_read` ให้สมาชิก
ทุกทีมในงานเห็นทุกแมตช์ของงานนั้น ผลจึงครบเสมอเมื่อถามทีละทัวร์นาเมนต์
แต่ถ้า LSN-0031 คิดคะแนนข้ามงาน แล้วเรียกฟังก์ชันนี้ในบริบทของผู้ใช้คนหนึ่ง
จะได้ชุดข้อมูลที่ไม่ครบโดยไม่มีอะไรฟ้อง — **บั๊กคลาสเดียวกับที่เจอในหน้า
ทัวร์นาเมนต์ของ LSN-0029** ที่นับ "ทุกทีมจ่ายแล้ว" จากแถวที่ RLS ยอมให้เห็น

### Gate หลังแก้

`db:reset` · `test:db` **190 ผ่าน** · `verify` (lint · tsc · 143 เทส) · `build` ผ่านทั้งหมด

**Verdict: PASS**
