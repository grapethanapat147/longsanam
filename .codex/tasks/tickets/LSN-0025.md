# 🟢 LSN-0025 — ผู้จัดยืนยันคอร์ตเอง

**Type:** Feature · **Phase:** 9 · **Status:** In Progress
**Source:** `docs/product-concept.md` หัวข้อ "เรื่องคอร์ต — คำถามที่ทำให้โปรดักต์นี้ตายได้"
**Depends on:** LSN-0021 (การคิดเงินจากราคาคอร์ต) — merge แล้ว

## Goal

ผู้จัดกดปุ่ม "ได้คอร์ตแล้ว" แล้วก๊วนกลายเป็น `booked` ได้ **โดยไม่ต้องมีสนามพันธมิตร**

นี่คือช่องว่างเดียวที่ขวางอยู่ระหว่าง MVP ที่เอกสารแนวคิดประกาศไว้ กับสิ่งที่ระบบ
ทำได้จริงวันนี้

## Context

ตรวจโค้ดแล้ว **มีเพียงสองทาง**ที่ทำให้ก๊วนกลายเป็น `booked`:

| ทาง | ที่ | เงื่อนไข |
| --- | --- | --- |
| `request_booking()` | `20260901000200_functions.sql:826` | สนามต้องเปิด `auto_confirm_bookings` |
| `venue_decide_booking()` | `20260901000200_functions.sql:883` | กั้นด้วย `is_venue_member()` |

ทั้งสองทางต้องมีคนฝั่งสนาม **ผู้จัดจึงยืนยันคอร์ตของตัวเองไม่ได้เลย** ทั้งที่ผู้จัด
ก๊วนแบดส่วนใหญ่มีคอร์ตประจำที่โทรจองเองอยู่แล้ว

**ครึ่งหลังมีอยู่แล้ว** — `settle_session_costs()` รองรับกรณี "สนามยืนยันทางโทรศัพท์
และไม่มีอะไรถูกจองในแอป" ไว้เรียบร้อย โดย comment ในโค้ดเขียนไว้ตรง ๆ ว่าให้ใช้
ราคาคอร์ตที่ถูกที่สุดที่ผู้จัดอนุมัติไว้เป็นฐานคิดเงิน ตั๋วนี้จึงเติมเฉพาะครึ่งแรก

**ข้อเท็จจริงอื่นที่ตรวจแล้ว:**

- `bookings.venue_id` และ `bookings.court_id` เป็น **NOT NULL** ทั้งคู่ —
  นี่คือข้อจำกัดจริงข้อเดียวของตั๋วนี้ ตัดสินแล้วให้เป็น nullable ดูหัวข้อ Decisions
- `bookings.price_thb` เป็น **NOT NULL** ผู้จัดจึงต้องกรอกราคาที่จ่ายจริง
  ไม่งั้นการคิดเงินจะไม่มีฐาน
- `bookings_read` policy ใช้ `is_session_organizer(session_id)` อยู่แล้ว
  ผู้จัดจึงเห็นแถวของตัวเองได้โดยไม่ต้องแก้ RLS แม้ `venue_id` จะว่าง
- `booking_status` enum มีค่า `confirmed` อยู่แล้ว ไม่ต้องเพิ่ม
- ทุกวันนี้ **ไม่มีการแจ้งเตือนผู้เล่นตอนได้คอร์ต** ทั้งสองทางเขียนแค่ `app_log`
  ผู้เล่นรู้จากการเปิดลิงก์ ซึ่งมี realtime อยู่แล้ว

## Scope

- RPC `confirm_court_manually()` ให้ผู้จัดบันทึกคอร์ตที่หามาเอง แล้วย้ายก๊วนไป `booked`
- ปุ่มฝั่งผู้จัดบนหน้าจัดการก๊วน พร้อมช่องกรอกชื่อสนามและราคาที่จ่ายจริง
- เขียน `audit_logs` ทั้ง `booking.confirmed` และ `session.booked` ให้ครบตามที่
  Definition of Done กำหนด
- pgTAP ปักขอบเขตสิทธิ์และ allowlist ของสถานะ

## Non-goals

- ไม่แตะทางของสนามพันธมิตรทั้งสองทางที่มีอยู่
- ไม่ทำ UI ให้สนามที่ไม่ได้เป็นพันธมิตรเข้ามาใช้
- ไม่เพิ่มการแจ้งเตือนตอนได้คอร์ต — ทางที่มีอยู่ก็ไม่แจ้ง ถ้าจะเพิ่มควรเพิ่มพร้อมกัน
  ทั้งสามทางในตั๋วแยก
- ไม่ทำการยกเลิกคอร์ตที่ยืนยันเอง — ใช้การยกเลิกก๊วนที่มีอยู่แล้ว

## สถานะไหนยืนยันได้ — allowlist ไม่ใช่ denylist

ด้วยเหตุผลเดียวกับบั๊กที่เคยเจอใน LSN-0020:

| สถานะ | ยืนยันได้ | เพราะ |
| --- | --- | --- |
| `ready_to_book` | ✅ | คนครบ เงินครบแล้ว เหลือแค่คอร์ต |
| `booking_failed` | ✅ | ระบบลองแล้วไม่ได้ ผู้จัดไปหามาเอง — เป็นเคสที่ตั๋วนี้มีไว้เพื่อ |
| `holding_court` | ❌ | มีคอร์ตที่กันไว้ค้างอยู่ ต้องปล่อยก่อน ไม่งั้นจะมีสองการจอง |
| `open` / `draft` | ❌ | **เงินยังไม่ครบ** การยอมให้ยืนยันตรงนี้คือการทำลายคำสัญญาหลักของโปรดักต์ — `ready_to_book` ปลอดภัยเพราะสถานะนั้นแปลว่าคนและเงินครบแล้วโดยนิยาม |
| `booked` / `cancelled` / `completed` | ❌ | จบไปแล้ว |

## Acceptance Criteria

- [x] ผู้จัดกดยืนยันคอร์ตจาก `ready_to_book` แล้วก๊วนเป็น `booked` — ปักด้วยเทส
- [x] ผู้จัดกดยืนยันคอร์ตจาก `booking_failed` แล้วก๊วนเป็น `booked` และ
      `failure_reason` ถูกล้าง — ปักด้วยเทส
- [x] สถานะนอก allowlist ถูกปฏิเสธทุกตัว รวม `open` ที่เงินยังไม่ครบ — ปักด้วยเทส
- [x] คนที่ไม่ใช่ผู้จัดก๊วนนั้นเรียกไม่ได้ — ปักด้วยเทส
- [x] ราคาที่ผู้จัดกรอกกลายเป็นฐานคิดเงินจริง: `settle_session_costs()` ต้องใช้ราคานั้น
      ไม่ใช่ราคาคอร์ตที่อนุมัติไว้ — ปักด้วยเทส **ข้อนี้คือข้อที่พิสูจน์ว่าตั๋วนี้ต่อกับ
      LSN-0021 ได้จริง**
- [x] เขียน `audit_logs` ทั้ง `booking.confirmed` และ `session.booked` โดย
      **from-state เป็นสถานะจริง** ไม่ใช่ `'holding_court'` ที่ hardcode ไว้ในทางเดิม
- [x] pgTAP ของ LSN-0021 และ LSN-0023 ยังเขียวโดยไม่ต้องแก้เทส
- [x] ปุ่มบนหน้าจัดการก๊วนขึ้นเฉพาะสถานะที่ยืนยันได้ ไม่ใช่ขึ้นแล้ว disabled
- [x] ราคาที่กรอกต้องมากกว่า 0 และถูกปฏิเสธที่ระดับฐานข้อมูลถ้าไม่ใช่

## Validation

- `npm run lint`, `npx tsc --noEmit`, `npm test` ผ่าน
- `npm run test:db` ผ่าน รวมข้อใหม่ และของ LSN-0019/0020/0021/0022/0023 ไม่แดง
- migration apply จาก `npm run db:reset` ได้
- ตรวจบนเบราว์เซอร์ 375×812: ผู้จัดยืนยันคอร์ตแล้วผู้เล่นเห็นสถานะเปลี่ยนเป็น
  "ยืนยันสนามเรียบร้อย" โดยไม่ต้องรีเฟรช (realtime มีอยู่แล้ว)
- ไม่มี UI ที่กดแล้วไม่ทำอะไร

## Decisions — ตัดสินครบแล้ว ไม่มีข้อค้าง

### 1. จะบันทึกคอร์ตที่ไม่ได้อยู่ในระบบยังไง → **ทาง ก. ทำให้ nullable** ✅ ตัดสินแล้ว 12 ก.ย.

เกรพเลือกทาง ก. — `bookings.venue_id` และ `bookings.court_id` กลายเป็น nullable
และคอร์ตที่ผู้จัดหามาเองถูกบันทึกเป็นแถว `bookings` ที่ไม่ผูกกับสนามในระบบ

**ทางที่ไม่ได้เลือก และเหตุผล:**

| ทาง | ทำไมไม่เลือก |
| --- | --- |
| ข. ให้เลือกสนามจากรายการที่มี | ใช้ไม่ได้กับสนามที่เราไม่รู้จัก ซึ่งคือเคสหลักของตั๋วนี้ |
| ค. ไม่สร้างแถว `bookings` เก็บเป็นโน้ตบน `sessions` | `settle_session_costs()` อ่านราคาจาก `bookings` จะต้องแก้ LSN-0021 ซึ่งมี pgTAP ปักไว้ 8 ข้อ |

**เหตุผลที่เลือกทาง ก.** วัด blast radius แล้วมีผู้อ่านสองคอลัมน์นี้เพียง 4 แห่ง
และสามแห่งแรกรับ null ได้โดยพฤติกรรมยังถูก:

| ที่ | พฤติกรรมเมื่อ null | ต้องแก้ไหม |
| --- | --- | --- |
| `rls_helpers.sql:160` `is_booking_venue_member` | คืน false — ไม่มีสนามไหนควรมาตัดสินการจองที่ผู้จัดทำเอง | ไม่ |
| `functions.sql:205` เช็คคอร์ตว่าง | ไม่ match — คอร์ตนอกระบบไม่ควรไปกันคิวคอร์ตในระบบ | ไม่ |
| `functions.sql:868` gate ของ `venue_decide_booking` | เข้าไม่ได้ | ไม่ |
| `src/app/admin/venues/page.tsx:25-32` รวมรายได้ตาม `venue_id` | ไม่พัง แต่ได้ Map ที่มีคีย์ `null` ซึ่งไม่มีใครอ่าน = ข้อมูลตายเงียบ ๆ และ type ประกาศไว้ว่า `venue_id: string` ซึ่งจะกลายเป็นคำโกหก | **ใช่** |

### สิ่งที่ต้องทำเพิ่มจากการตัดสินนี้

1. **Migration** เปลี่ยน `bookings.venue_id` และ `bookings.court_id` เป็น nullable
2. **เพิ่มคอลัมน์ `manual_venue_name text`** บน `bookings` สำหรับชื่อสนามที่ผู้จัดพิมพ์
   เพื่อให้หน้าก๊วนและใบสรุปมีอะไรแสดง
3. **check constraint** ที่บังคับว่าแถวหนึ่งต้องเป็นอย่างใดอย่างหนึ่งชัดเจน ไม่ใช่ครึ่ง ๆ:
   ```sql
   check (
     (venue_id is not null and court_id is not null and manual_venue_name is null)
     or
     (venue_id is null and court_id is null and manual_venue_name is not null)
   )
   ```
   ข้อนี้สำคัญกว่าที่ดู — ถ้าไม่มี จะมีแถวที่มี `venue_id` แต่ไม่มี `court_id` โผล่มา
   ทีหลังโดยไม่มีใครตั้งใจ แล้วไม่มีใครรู้ว่าควรอ่านยังไง
4. **แก้ `src/app/admin/venues/page.tsx`** เปลี่ยน type เป็น `venue_id: string | null`
   แล้วข้ามแถวที่เป็น null อย่างชัดเจน ไม่ใช่ปล่อยให้ตกลง Map เงียบ ๆ
5. **เทสที่ปัก check constraint** ว่าแถวครึ่ง ๆ ถูกปฏิเสธจริง — ปักด้วย `throws_ok`

### Acceptance Criteria เพิ่มจากการตัดสินนี้

- [x] แถว `bookings` ที่มี `venue_id` แต่ไม่มี `court_id` ถูกปฏิเสธที่ระดับฐานข้อมูล — ปักด้วยเทส
- [x] แถวที่ไม่มีทั้งสนามในระบบและไม่มี `manual_venue_name` ถูกปฏิเสธ — ปักด้วยเทส
- [x] หน้ารวมรายได้ฝั่งแอดมินไม่นับการจองที่ผู้จัดยืนยันเอง และไม่พัง — ตรวจแล้ว
      สร้าง manual booking ฿840 ที่ก๊วนของพระราม 9 พิคเคิลบอล แล้วเปิดหน้าแอดมิน
      สนามนั้นยังแสดง **฿0** ส่วนทองหล่อที่มีการจองจริงแสดง ฿3,920 และหน้าไม่พัง

### 2. ผู้จัดโกหกว่าได้คอร์ตแล้ว → **ไม่แก้ในรอบนี้** ✅ ตัดสินแล้ว

เอกสารแนวคิดบันทึกเป็นความเสี่ยงไว้แล้ว ตั๋วนี้ **ไม่แก้** โดยตั้งใจ
เพราะกลไกที่มีอยู่รองรับผลเสียแล้ว: ผู้เล่นยกเลิกได้และได้เงินคืนตามนโยบายของ
LSN-0019 ส่วนพฤติกรรมซ้ำของผู้จัดจะเห็นได้จาก `audit_logs`

ถ้าจะกันจริงในอนาคต ทางที่สมเหตุสมผลคือให้ผู้เล่นคนใดคนหนึ่งยืนยันรับทราบ
ซึ่งเป็นงานคนละใบ

## Blocking notes

- ไม่มีตั๋วอื่นบล็อก — LSN-0021 merge แล้ว
- **ไม่มี Open Question เหลือ** ทั้งสองข้อตัดสินแล้ว — พร้อม `/kanban plan LSN-0025`
- LSN-0024 (รายการเก็บเงินเพิ่ม) ไม่ทับกัน ทำคู่ขนานได้
- ตั๋วนี้ทำให้ `docs/product-concept.md` พูดความจริง ตอนนี้เอกสารระบุไว้ตรง ๆ ว่า
  ทางที่ 1 "ยังทำไม่ได้" เมื่อตั๋วนี้ done ต้องกลับไปแก้บรรทัดนั้นด้วย

## Implementation plan

เขียน 12 ก.ย. 2569 · ตัดสินก่อนเขียนสองข้อ: (ก) เดินหน้าเขียนแผน
(ข) **เปิดทาง `ready_to_book → booked` แล้วแก้เทส state machine ให้ตรงกติกาใหม่**

ตั๋วยังอยู่ **Backlog** เพราะ flow ของ longsanam ไม่มี state `Plan` เหมือน LSN-0023/0024

### ของที่ตรวจเพิ่มตอนเขียนแผน และเปลี่ยนรูปงาน

| พบอะไร | ผลต่อแผน |
| --- | --- |
| `tests/state-machines.test.ts:37-40` ชื่อ `'refuses to skip the holding step'` ปักว่า `ready_to_book → booked` เป็น **false** | ต้องแก้เทสนี้ ไม่ใช่หลบ — Task 3 |
| `bookings_no_overlap` เป็น `EXCLUDE ... (court_id WITH =, ...)` | `court_id` เป็น null แล้วไม่มีวันชน = การจองที่ผู้จัดทำเองไม่ไปกันคิวคอร์ตในระบบ **ถูกแล้ว** แต่ต้องปักด้วยเทส |
| `bookings_price_thb_check` คือ `price_thb >= 0` | **ยอม 0 ได้** ทางใหม่ต้องมี guard ของตัวเอง ไม่งั้น AC เรื่องราคา > 0 จะไม่มีอะไรบังคับ |
| `bookings.idempotency_key` เป็น **UNIQUE NOT NULL** | สร้าง key แบบคาดเดาได้ `'manual:' || session_id` แล้วการกดซ้ำจะชนกันเองโดยอัตโนมัติ |
| `page.tsx:289` `SettleControl` อ่าน `bookings.find(b => b.status === 'confirmed')?.price_thb` | ราคาที่ผู้จัดกรอกจะไหลเข้าหน้าสรุปค่าใช้จ่ายทันทีโดยไม่ต้องแก้อะไร |
| `page.tsx:215` มี branch ของ `booking_failed` อยู่แล้ว | เป็นที่ของปุ่มใหม่ ไม่ต้องรื้อ layout |
| `assertSessionTransition` ถูกเรียกที่เดียวคือ `session.ts:202` | ตาราง transition เป็น **เอกสาร** มากกว่าด่านบังคับ ด่านจริงคือ allowlist ใน SQL — แผนนี้ทำทั้งสองอย่างให้ตรงกัน |

---

### Task 1 — สคีมา: nullable + รูปแถวที่ถูกต้อง

**Files:** Create `supabase/migrations/20260914000100_manual_booking.sql`

- [ ] **Step 1: เขียน migration**

```sql
-- ผู้จัดยืนยันคอร์ตเอง (LSN-0025)
--
-- คอร์ตที่ผู้จัดโทรจองเองไม่มีอยู่ในตาราง venues/courts ของเรา สองคอลัมน์นี้
-- จึงต้องว่างได้ วัดผลกระทบไว้ในตั๋วแล้ว: มีผู้อ่านสองคอลัมน์นี้ 4 แห่ง
-- สามแห่งแรกรับ null ได้โดยพฤติกรรมยังถูก เหลือหน้ารวมรายได้ฝั่งแอดมินที่ต้องแก้

alter table public.bookings
  alter column venue_id drop not null,
  alter column court_id drop not null,
  add column if not exists manual_venue_name text;

-- nullable สองคอลัมน์พร้อมกันเปิดรูปแถวขึ้นมาสี่แบบ ซึ่งสองแบบไม่มีความหมาย
-- ถ้าไม่กั้นตรงนี้ จะมีแถวที่มี venue_id แต่ไม่มี court_id โผล่มาทีหลัง
-- โดยไม่มีใครตั้งใจ แล้วไม่มีใครรู้ว่าควรอ่านยังไง
alter table public.bookings
  add constraint bookings_venue_shape check (
    (venue_id is not null and court_id is not null and manual_venue_name is null)
    or
    (venue_id is null and court_id is null
       and manual_venue_name is not null
       and length(btrim(manual_venue_name)) between 1 and 120)
  );

-- bookings_price_thb_check เดิมคือ price_thb >= 0 ซึ่งยอมให้เป็น 0
-- การจองที่ผู้จัดกรอกเองเป็นฐานคิดเงินของทั้งก๊วน ฿0 จึงไม่มีความหมาย
alter table public.bookings
  add constraint bookings_manual_price_positive check (
    manual_venue_name is null or price_thb > 0
  );

comment on column public.bookings.manual_venue_name is
  'ชื่อสนามที่ผู้จัดพิมพ์เอง มีค่าเมื่อคอร์ตไม่ได้อยู่ในระบบ · null สำหรับการจองผ่านสนามพันธมิตร';
```

- [ ] **Step 2: เทสที่ปักรูปแถว**

สร้าง `supabase/tests/manual_booking.test.sql` เริ่มที่ `select plan(4);`

```sql
-- แถวครึ่ง ๆ ต้องถูกปฏิเสธ ทั้งสองทิศ
select throws_ok($$
  insert into public.bookings (session_id, venue_id, court_id, starts_at, ends_at,
    status, price_thb, attempt_no, idempotency_key)
  values ((select id from public.sessions where public_code='READY01'),
    (select id from public.venues limit 1), null,
    now(), now() + interval '2 hours', 'confirmed', 500, 1, 'shape-test-1')
$$, '23514', null, 'มีสนามแต่ไม่มีคอร์ต ถูกปฏิเสธ');

select throws_ok($$
  insert into public.bookings (session_id, starts_at, ends_at,
    status, price_thb, attempt_no, idempotency_key)
  values ((select id from public.sessions where public_code='READY01'),
    now(), now() + interval '2 hours', 'confirmed', 500, 1, 'shape-test-2')
$$, '23514', null, 'ไม่มีทั้งสนามในระบบและไม่มีชื่อสนามที่พิมพ์เอง ถูกปฏิเสธ');

-- ราคาศูนย์ของการจองแบบกรอกเอง ถูกปฏิเสธ แม้ check เดิมจะยอม >= 0
select throws_ok($$
  insert into public.bookings (session_id, manual_venue_name, starts_at, ends_at,
    status, price_thb, attempt_no, idempotency_key)
  values ((select id from public.sessions where public_code='READY01'),
    'คอร์ตแบดลุงหมี', now(), now() + interval '2 hours', 'confirmed', 0, 1, 'shape-test-3')
$$, '23514', null, 'การจองที่กรอกเอง ราคา ฿0 ถูกปฏิเสธ');

-- คอร์ตนอกระบบไม่ไปกันคิวคอร์ตในระบบ เพราะ exclusion ใช้ court_id WITH =
-- และ null ไม่เท่ากับอะไรเลย · ข้อนี้พิสูจน์ว่าสองแถวเวลาทับกันอยู่ร่วมกันได้
select lives_ok($$
  insert into public.bookings (session_id, manual_venue_name, starts_at, ends_at,
    status, price_thb, attempt_no, idempotency_key)
  select (select id from public.sessions where public_code='READY01'),
         'คอร์ตแบดลุงหมี', b.starts_at, b.ends_at, 'confirmed', 500, 1, 'shape-test-4'
  from public.bookings b where b.status = 'confirmed' limit 1
$$, 'การจองที่กรอกเองไม่ชนกับคอร์ตในระบบที่เวลาทับกัน');
```

- [ ] **Step 3: รัน**

```bash
npm run db:reset && npm run test:db
```
คาดหวัง: ไฟล์ใหม่ผ่าน 4 ข้อ · ไฟล์เดิมทั้ง 6 ไม่แดง

- [ ] **Step 4: commit**

```bash
git add supabase/migrations/20260914000100_manual_booking.sql supabase/tests/manual_booking.test.sql
git commit -m "feat(LSN-0025): let a booking point at a court we do not know"
```

---

### Task 2 — RPC `confirm_court_manually()`

**Files:** Modify `supabase/migrations/20260914000100_manual_booking.sql`

- [ ] **Step 1: เขียนฟังก์ชัน**

```sql
create or replace function public.confirm_court_manually(
  p_session_id  uuid,
  p_venue_name  text,
  p_price_thb   integer
) returns jsonb
language plpgsql security definer
set search_path = public, pg_temp
as $$
declare
  v_session public.sessions%rowtype;
  v_from    text;
  v_id      uuid;
begin
  select * into v_session from public.sessions where id = p_session_id for update;
  if v_session.id is null then
    return jsonb_build_object('ok', false, 'reason', 'session_not_found');
  end if;
  if not public.is_session_organizer(p_session_id) then
    return jsonb_build_object('ok', false, 'reason', 'not_organizer');
  end if;

  -- allowlist ไม่ใช่ denylist ด้วยเหตุผลเดียวกับบั๊ก LSN-0020
  --
  -- แถวที่สำคัญที่สุดของด่านนี้คือแถวที่ปฏิเสธ `open`: ตอนนั้นเงินยังไม่ครบ
  -- การยอมให้ยืนยันคอร์ตตรงนั้นคือการทำลายคำสัญญาหลักของโปรดักต์ด้วยปุ่มเดียว
  -- ส่วน holding_court ถูกปฏิเสธเพราะมีคอร์ตที่กันไว้ค้างอยู่ ต้องปล่อยก่อน
  if v_session.status not in ('ready_to_book', 'booking_failed') then
    return jsonb_build_object('ok', false, 'reason', 'session_not_ready');
  end if;

  if p_price_thb is null or p_price_thb <= 0 then
    return jsonb_build_object('ok', false, 'reason', 'invalid_amount');
  end if;
  if p_venue_name is null or length(btrim(p_venue_name)) = 0 then
    return jsonb_build_object('ok', false, 'reason', 'venue_name_required');
  end if;

  v_from := v_session.status::text;

  -- idempotency_key เป็น UNIQUE อยู่แล้ว การกดซ้ำจึงชนกันเองโดยไม่ต้องเขียน guard
  insert into public.bookings
    (session_id, manual_venue_name, starts_at, ends_at, status,
     price_thb, attempt_no, idempotency_key, confirmed_at, decided_by)
  values
    (p_session_id, btrim(p_venue_name), v_session.starts_at, v_session.ends_at,
     'confirmed', p_price_thb, 1, 'manual:' || p_session_id::text, now(), auth.uid())
  returning id into v_id;

  update public.sessions
  set status = 'booked', booking_id = v_id, failure_reason = null
  where id = p_session_id;

  perform public.app_log(auth.uid(), 'booking', v_id, p_session_id,
    'booking.confirmed', null, 'confirmed',
    jsonb_build_object('manualVenueName', btrim(p_venue_name),
                       'priceThb', p_price_thb, 'manual', true));

  -- from-state เป็นสถานะจริง ไม่ใช่ 'holding_court' ที่ hardcode ไว้ในทางเดิม
  -- เพราะทางนี้มาจาก ready_to_book หรือ booking_failed
  perform public.app_log(auth.uid(), 'session', p_session_id, p_session_id,
    'session.booked', v_from, 'booked',
    jsonb_build_object('bookingId', v_id, 'manual', true));

  return jsonb_build_object('ok', true, 'bookingId', v_id, 'priceThb', p_price_thb);
exception when unique_violation then
  return jsonb_build_object('ok', false, 'reason', 'already_confirmed');
end;
$$;

revoke execute on function public.confirm_court_manually(uuid, text, integer) from public;
grant execute on function public.confirm_court_manually(uuid, text, integer)
  to authenticated, service_role;
```

**ไม่มีการแจ้งเตือนผู้เล่นในฟังก์ชันนี้โดยตั้งใจ** — ทางเดิมทั้งสองทางก็ไม่แจ้ง
เขียนแค่ `app_log` ผู้เล่นรู้จาก realtime ถ้าจะเพิ่มควรเพิ่มพร้อมกันทั้งสามทางในตั๋วแยก

- [ ] **Step 2: เทส pgTAP ต่อจาก Task 1 → `select plan(10);`**

เพิ่มอีก 6 ข้อ:

5. ผู้จัดยืนยันจาก `ready_to_book` → ก๊วนเป็น `booked`
6. ผู้จัดยืนยันจาก `booking_failed` → `booked` และ `failure_reason` ถูกล้างเป็น null
7. จาก `open` → `session_not_ready` **และก๊วนยังเป็น `open`**
8. จาก `holding_court` → `session_not_ready`
9. คนที่ไม่ใช่ผู้จัด → `not_organizer` (`set local role authenticated` + jwt ของคนอื่น)
10. **ราคาที่กรอกกลายเป็นฐานคิดเงินจริง** — ยืนยันคอร์ตที่ ฿900 แล้วเรียก
    `settle_session_costs()` ต้องได้ `totalThb` ที่รวม ฿900 นั้น ไม่ใช่ราคาคอร์ต
    ที่อนุมัติไว้ **ข้อนี้คือข้อที่พิสูจน์ว่าครึ่งแรกกับครึ่งหลังต่อกันติด**

- [ ] **Step 3: falsify ทีละอัน**

| แก้อะไร | ข้อที่ต้องแดง |
| --- | --- |
| เอา `status not in (...)` ออก | 7, 8 |
| เปลี่ยน allowlist เป็น denylist `status <> 'cancelled'` | 7, 8 |
| เอา `is_session_organizer` ออก | 9 |
| ไม่ตั้ง `failure_reason = null` | 6 |
| ใส่ `price_thb` เป็นค่าคงที่แทนค่าที่รับมา | 10 |

ถ้าข้อไหนไม่แดง ให้เขียนเทสข้อนั้นใหม่ก่อนไปต่อ

- [ ] **Step 4: `npm run db:reset && npm run test:db` แล้ว commit**

---

### Task 3 — state machine ให้ตรงกับกติกาใหม่

**Files:** Modify `src/lib/domain/state-machines.ts` · `tests/state-machines.test.ts`

- [ ] **Step 1: เปิดทางใหม่**

```ts
  // booked ตรงนี้คือทางที่ผู้จัดหาคอร์ตมาเอง (LSN-0025) ไม่ใช่การข้ามขั้นตอน
  // ขั้น holding มีไว้กันคอร์ต *ในระบบ* ไว้ระหว่างรอสนามตอบ คอร์ตที่ผู้จัด
  // โทรจองเองไม่มีอะไรให้กัน
  ready_to_book: ['holding_court', 'booked', 'open', 'cancelled'],
  holding_court: ['booked', 'booking_failed', 'cancelled'],
  booked: ['completed', 'cancelled'],
  booking_failed: ['holding_court', 'booked', 'open', 'cancelled'],
```

- [ ] **Step 2: เขียนเทสใหม่แทนข้อเดิม ไม่ใช่ลบทิ้ง**

ข้อเดิมชื่อ `'refuses to skip the holding step'` ที่ `tests/state-machines.test.ts:37`
ยืนยันสองอย่างพร้อมกัน และ **ข้อที่สองยังต้องจริงอยู่**

```ts
  it('lets the organizer skip the holding step, but never the money gate', () => {
    // LSN-0025: ขั้น holding กันคอร์ตในระบบไว้ระหว่างรอสนามตอบ
    // คอร์ตที่ผู้จัดโทรจองเองไม่มีอะไรให้กัน จึงข้ามได้
    expect(canTransitionSession('ready_to_book', 'booked')).toBe(true);
    expect(canTransitionSession('booking_failed', 'booked')).toBe(true);

    // แต่ open แปลว่าเงินยังไม่ครบ ทางนี้ต้องปิดตลอดกาล ไม่ว่าใครจะยืนยันอะไร
    expect(canTransitionSession('open', 'booked')).toBe(false);
    expect(canTransitionSession('draft', 'booked')).toBe(false);
  });
```

- [ ] **Step 3: รัน แล้วดูว่าเทสอื่นไม่พัง**

```bash
npx vitest run tests/state-machines.test.ts
```
คาดหวัง: ผ่านทั้งไฟล์ · ข้อ `'walks the happy path'` ที่ไล่
`draft → open → ready_to_book → holding_court → booked` ต้องยังผ่าน
เพราะการเพิ่มปลายทางไม่ได้ลบของเดิม

- [ ] **Step 4: commit**

```bash
git commit -am "feat(LSN-0025): the holding step is optional, the money gate is not"
```

---

### Task 4 — ปิดช่องที่หน้ารวมรายได้ฝั่งแอดมิน

**Files:** Modify `src/app/admin/venues/page.tsx:25-32`

- [ ] **Step 1: แก้ type ที่กลายเป็นคำโกหก และข้าม null อย่างชัดเจน**

```tsx
  const revenue = new Map<string, number>();
  for (const booking of (bookings ?? []) as {
    venue_id: string | null;
    status: string;
    price_thb: number;
  }[]) {
    // การจองที่ผู้จัดยืนยันเองไม่มีสนามในระบบ จึงไม่เป็นรายได้ของสนามไหนเลย
    // ข้ามอย่างเปิดเผย ไม่ใช่ปล่อยให้ตกลง Map เป็นคีย์ null ที่ไม่มีใครอ่าน
    if (booking.venue_id === null) continue;
    if (booking.status === 'confirmed') {
      revenue.set(booking.venue_id, (revenue.get(booking.venue_id) ?? 0) + booking.price_thb);
    }
  }
```

- [ ] **Step 2: `npx tsc --noEmit` แล้ว commit**

---

### Task 5 — server action และ UI ฝั่งผู้จัด

**Files:**
- Create `src/components/manual-court-control.tsx`
- Modify `src/lib/actions/session.ts` · `src/app/organizer/sessions/[id]/page.tsx` · `src/i18n/th.ts`

- [ ] **Step 1: action**

`confirmCourtManuallyAction(sessionId, venueName, priceThb)` เรียกผ่าน
`createClient()` ของผู้จัดเอง **ไม่ใช่ admin client** เพื่อให้ `is_session_organizer()`
ข้างใน RPC เห็นผู้เรียกจริง — แบบเดียวกับ `settleSessionAction` ที่
`src/lib/actions/session.ts:395`

เพิ่มคำแปลเข้า `reasonLabel`: `session_not_ready`, `venue_name_required`,
`already_confirmed`

- [ ] **Step 2: UI**

วางในหน้า `src/app/organizer/sessions/[id]/page.tsx` ซึ่งมี branch ของ
`booking_failed` อยู่แล้วที่บรรทัด 215

- ขึ้นเฉพาะ `session.status === 'ready_to_book' || session.status === 'booking_failed'`
  — **ไม่ใช่ขึ้นตลอดแล้ว disabled** ตาม Definition of Done
- สองช่อง: ชื่อสนาม (บังคับ) และราคาที่จ่ายจริง (บังคับ > 0)
- ข้อความใต้ปุ่มบอกตรง ๆ ว่าราคานี้จะถูกใช้เป็นฐานหารค่าใช้จ่ายของทั้งก๊วน
  เพราะผู้จัดต้องรู้ว่าเลขนี้ไปโผล่ที่ไหน

- [ ] **Step 3: `npm run verify` แล้ว commit**

---

### Task 6 — ตรวจของจริงแล้วปิด

- [ ] **Step 1: gate เต็ม**

```bash
npm run db:reset && npm run test:db && npm run verify && npm run build
```

- [ ] **Step 2: ตรวจบนเบราว์เซอร์ 375×812**

ต้องสร้างก๊วนสถานะ `ready_to_book` ขึ้นมาก่อน (seed มี `READY01` อยู่แล้ว)

| ตรวจ | ต้องได้ |
| --- | --- |
| ผู้จัดกดยืนยันคอร์ต | ก๊วนเป็น `booked` · หน้าแสดงชื่อสนามที่พิมพ์ |
| ผู้เล่นเปิดหน้าก๊วนค้างไว้ | เห็นสถานะเปลี่ยนโดยไม่ต้องรีเฟรช (realtime มีอยู่แล้ว) |
| หน้าสรุปค่าใช้จ่ายของผู้จัด | `SettleControl` ขึ้นราคาที่กรอก เพราะมันอ่าน `bookings.price_thb` ของแถว `confirmed` อยู่แล้ว |
| ก๊วนสถานะ `open` | **ไม่มีปุ่มนี้เลย** |
| หน้ารวมรายได้ฝั่งแอดมิน | ไม่พัง และไม่นับการจองนี้เป็นรายได้ของสนามไหน |

- [ ] **Step 3: ติ๊ก Acceptance Criteria เฉพาะข้อที่พิสูจน์ได้จริง**

ข้อไหนยังไม่ได้เห็นกับตา ให้สร้างสถานการณ์ขึ้นมาดูก่อน — บทเรียนจาก LSN-0023

- [ ] **Step 4: กลับไปแก้ `docs/product-concept.md`**

ตอนนี้เอกสารเขียนไว้ว่าทางที่ 1 **"ยังทำไม่ได้"** และอธิบายช่องว่างไว้
เมื่อตั๋วนี้เสร็จ บรรทัดนั้นกลายเป็นเท็จ ต้องแก้ให้ตรงในตั๋วเดียวกัน ไม่ใช่ทิ้งไว้

- [ ] **Step 5: รอ `/kanban move-review LSN-0025`**

---

### ความเสี่ยงที่รู้ตัวอยู่

**Task 1 แก้คอลัมน์ NOT NULL ที่มีข้อมูลอยู่แล้ว** การ drop not null ไม่ทำให้ข้อมูลเดิม
เสียหาย แต่ `bookings_venue_shape` เป็น constraint ใหม่ที่ **จะตรวจแถวเดิมทั้งหมด**
ตอน apply ถ้ามีแถวไหนไม่เข้ารูป migration จะล้ม — ซึ่งดีกว่าผ่านไปเงียบ ๆ
seed ปัจจุบันมีแต่แถวที่มีทั้ง venue และ court จึงควรผ่าน แต่ให้ดูผลรัน ไม่ใช่เดา

**`'manual:' || session_id` ทำให้ยืนยันคอร์ตซ้ำไม่ได้ตลอดกาล** ถ้าผู้จัดยืนยันผิด
แล้วอยากแก้ ทางเดียวคือยกเลิกก๊วน ซึ่งเป็น non-goal ที่ตั๋วเขียนไว้แล้ว
ถ้ากลายเป็นปัญหาจริงให้เปิดตั๋วแก้คอร์ต ไม่ใช่ถอด unique ออก

**ตาราง `SESSION_TRANSITIONS` ไม่ใช่ด่านบังคับ** มีผู้เรียก `assertSessionTransition`
อยู่ที่เดียว และไม่ได้อยู่บนเส้นทางนี้ ด่านจริงคือ allowlist ใน SQL — Task 3
ทำให้เอกสารตรงกับความจริง ไม่ได้เพิ่มการป้องกัน อย่าสับสนสองอย่างนี้
