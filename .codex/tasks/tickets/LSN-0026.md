# 🟢 LSN-0026 — ก๊วนที่คงอยู่ข้ามครั้ง

**Type:** Feature · **Phase:** 10 · **Status:** In Progress
**Spec:** `.codex/specs/tournaments.md` หัวข้อ Decisions ข้อ 1
**Depends on:** LSN-0027 — **merge เข้า main แล้ว 17 ก.ย. 2569 บล็อกหลุดแล้ว**

## Goal

ก๊วนกลายเป็น *กลุ่มคน* ที่คงอยู่ข้ามการนัดเล่น ไม่ใช่ชื่อของการนัดเล่นครั้งเดียว
ผู้จัดตั้งนัดใหม่ให้ก๊วนเดิมได้โดยไม่ต้องชวนคนใหม่ทุกครั้ง

## Context

ตรวจฐานข้อมูลจริงแล้ว **ไม่มีตาราง `groups` / `teams` / `tournaments` เลย** และ
`sessions` ไม่มีคอลัมน์ไหนผูกนัดนี้กับนัดหน้า (ไม่มี `recurring`, `group`, `parent`,
`series`) — "ก๊วน" ในระบบวันนี้คือ `sessions` หนึ่งแถว = การนัดเล่นหนึ่งครั้ง

ตั๋วนี้เป็นใบแรกของทิศทางทัวร์นาเมนต์ และ **มีคุณค่าของตัวเองตั้งแต่วันแรก
แม้ทัวร์นาเมนต์จะไม่เกิดขึ้นเลย** — ผู้จัดก๊วนแบดประจำตั้งนัดสัปดาห์หน้าได้
โดยไม่ต้องไปตามชวนทีละคนในกลุ่ม LINE ซ้ำทุกอาทิตย์

**ของที่ใช้ซ้ำได้:** `sessions.public_code` มีกลไกสร้างโค้ดอยู่แล้วคือ
`generate_session_code()` และ trigger `sessions_assign_code()` ลิงก์เชิญเข้าก๊วน
ใช้รูปแบบเดียวกันได้ ไม่ต้องคิดใหม่

## Scope

- ตาราง `groups` และ `group_members`
- `sessions.group_id` **nullable** — นัดผูกกับก๊วนก็ได้ ไม่ผูกก็ได้
- สร้างก๊วน · เชิญด้วยลิงก์ · เข้าร่วม · ออกจากก๊วน
- ตั้งนัดจากก๊วน แล้วสมาชิกในก๊วนได้รับแจ้งเตือน
- หน้ารายการก๊วนของฉัน และหน้าก๊วนหนึ่งก๊วน

## Non-goals

- ไม่ทำทัวร์นาเมนต์ · ไม่ทำคะแนนฝีมือ · ไม่ทำเพื่อน — เป็นตั๋วถัดไปตาม spec
- ไม่ทำนัดซ้ำอัตโนมัติตามตาราง (recurring schedule) — ตั๋วนี้แค่ทำให้*กลุ่มคน*คงอยู่
  ส่วนการตั้งนัดยังกดเอง
- ไม่ทำแชตในก๊วน — LINE ทำหน้าที่นั้นอยู่แล้วและดีกว่า
- ไม่ทำรูปปกก๊วน ไม่ทำบทบาทมากกว่า owner/member
- ไม่บังคับว่าต้องมีก๊วนก่อนถึงตั้งนัดได้

## Decisions — ตัดสินแล้ว

### 1. ก๊วนเป็นของ **ไม่บังคับ**

`sessions.group_id` เป็น nullable และการตั้งนัดแบบไม่มีก๊วนต้องใช้ได้เหมือนเดิมทุกอย่าง

เหตุผล: คุณสมบัติที่เป็นหัวใจของโปรดักต์คือ **ลิงก์เดียวที่ใครก็กดเข้ามาได้โดยไม่ต้อง
สมัครอะไรก่อน** ถ้าบังคับให้มีก๊วนก่อน จะเพิ่มขั้นตอนก่อนถึงคุณค่าแรก ซึ่งเป็นสิ่งที่
เอกสารแนวคิดเขียนไว้ว่าตั้งใจหลีกเลี่ยง

### 2. ลบก๊วนแล้วนัดเก่าต้องไม่หาย

`sessions.group_id` ใช้ `on delete set null` และก๊วนใช้ **archive ไม่ใช่ลบจริง**

เหตุผล: นัดหนึ่งนัดมีเงิน การจ่าย การคืนเงิน และใบสรุปผูกอยู่ ประวัติเหล่านั้นต้อง
อธิบายได้ต่อให้ก๊วนเลิกไปแล้ว

### 3. เจ้าของก๊วนคนเดียวใน v1

`group_members.role` เป็น `owner | member` และมี owner ได้คนเดียวต่อก๊วน
ผู้ร่วมเป็นเจ้าของหลายคนเป็นงานคนละใบ เพราะเปิดคำถามเรื่องใครลบใครได้

### 4. คนหนึ่งอยู่ได้หลายก๊วน

ไม่มีข้อจำกัด — คนเล่นแบดหลายก๊วนเป็นเรื่องปกติ และเป็นสิ่งที่ทำให้
"เพิ่มเพื่อนจากก๊วนอื่น" ในตั๋วถัดไปมีความหมาย

## Design

```sql
create table public.groups (
  id            uuid primary key default gen_random_uuid(),
  public_code   text not null unique,        -- ลิงก์เชิญ ใช้รูปแบบเดียวกับ sessions
  name          text not null,
  sport_id      uuid not null references public.sports(id),
  home_district text,
  created_by    uuid not null references public.profiles(id),
  archived_at   timestamptz,                 -- ไม่ลบจริง
  created_at    timestamptz not null default now(),
  updated_at    timestamptz not null default now()
);

create table public.group_members (
  group_id  uuid not null references public.groups(id) on delete cascade,
  user_id   uuid not null references public.profiles(id) on delete cascade,
  role      text not null check (role in ('owner', 'member')),
  joined_at timestamptz not null default now(),
  primary key (group_id, user_id)
);

-- เจ้าของคนเดียวต่อก๊วน
create unique index groups_single_owner
  on public.group_members (group_id) where role = 'owner';

alter table public.sessions
  add column if not exists group_id uuid references public.groups(id) on delete set null;
```

## Acceptance Criteria

- [ ] สร้างก๊วนแล้วผู้สร้างกลายเป็น `owner` โดยอัตโนมัติ — ปักด้วยเทส
- [ ] ก๊วนหนึ่งก๊วนมี `owner` ได้คนเดียว การเพิ่มคนที่สองเป็น owner ถูกปฏิเสธ
      ที่ระดับฐานข้อมูล — ปักด้วยเทส
- [ ] เข้าร่วมก๊วนผ่านลิงก์ `public_code` ได้ และเข้าซ้ำไม่สร้างแถวซ้ำ — ปักด้วยเทส
- [ ] สมาชิกออกจากก๊วนได้ · **เจ้าของออกไม่ได้** จนกว่าจะโอนหรือ archive ก๊วน — ปักด้วยเทส
- [ ] **ตั้งนัดโดยไม่มีก๊วนยังทำได้เหมือนเดิมทุกอย่าง** — ปักด้วยเทส
      ข้อนี้คือข้อที่กันไม่ให้ตั๋วนี้ทำลายทางเข้าหลักของโปรดักต์
- [ ] ตั้งนัดจากก๊วน แล้วสมาชิกทุกคนได้ `notifications` **คนละหนึ่งแถว**
      ไม่รวมผู้ตั้งเอง — ปักด้วยเทส
- [ ] archive ก๊วนแล้วนัดเก่ายังอยู่ครบและเปิดดูได้ — ปักด้วยเทส
- [ ] คนนอกก๊วนอ่าน `group_members` ตรง ๆ ไม่ได้ — ปักด้วย pgTAP
- [ ] pgTAP ของ LSN-0019/0020/0021/0022/0023 ยังเขียวโดยไม่ต้องแก้เทส
- [ ] การสร้าง เข้าร่วม ออก และ archive เขียน `audit_logs`
- [ ] ไม่มี UI ที่กดแล้วไม่ทำอะไร

## Validation

- `npm run lint`, `npx tsc --noEmit`, `npm test` ผ่าน
- `npm run test:db` ผ่าน รวมข้อใหม่ และของเดิมไม่แดง
- migration apply จาก `npm run db:reset` ได้
- ตรวจบนเบราว์เซอร์ 375×812 ด้วยสามมุมมอง: เจ้าของก๊วน · สมาชิก · คนนอกที่กดลิงก์เชิญ
- seed มีก๊วนตัวอย่างอย่างน้อยหนึ่งก๊วนที่มีสมาชิกและมีนัดผูกอยู่

## Implementation plan

**วันที่วางแผน:** 17 ก.ย. 2569 · **สาขา:** `feature/LSN-0026`

### ตรวจของจริงก่อนวางแผนแล้ว

| ที่ตั๋วอ้าง | สถานะจริง |
| --- | --- |
| `generate_session_code()` · trigger `sessions_assign_code()` | มีอยู่จริงที่ `20260901000200_functions.sql:103` และ `:124` |
| ตาราง `groups` / `group_members` | ยังไม่มีในฐานข้อมูล |
| แบบแผน RLS helper | `is_session_organizer` / `is_session_participant` ที่ `20260901000150_rls_helpers.sql` — `security definer`, `stable`, `set search_path` |
| ทางสร้างนัดวันนี้ | `createSessionAction` ที่ `src/lib/actions/session.ts:59` เขียน `sessions` ตรง ๆ ไม่ผ่าน RPC |

### สามเรื่องที่แผนนี้ตัดสินให้ เพราะตั๋วไม่ได้ตัดสิน

**1. โค้ดของก๊วนต้องใช้ฟังก์ชันใหม่ ไม่ใช่เรียก `generate_session_code()`**

ฟังก์ชันเดิม `exit when not exists (select 1 from public.sessions ...)` —
มันตรวจซ้ำกับ **ตาราง sessions เท่านั้น** เอามาใช้กับ `groups` ตรง ๆ จะได้โค้ดที่
ไม่การันตีว่าไม่ซ้ำในตารางของตัวเอง → เขียน `generate_group_code()` แยก
ใช้ตัวอักษรชุดเดียวกัน ความยาวเท่ากัน

**โค้ดก๊วนกับโค้ดนัดชนกันได้ และยอมรับได้** เพราะคนละ route
(`/s/<code>` กับ `/g/<code>`) ไม่ต้องตรวจข้ามตาราง

**2. RLS ของ `group_members` จะวนลูปถ้าเขียนตรงไปตรงมา**

policy ที่ว่า "อ่านสมาชิกได้ถ้าคุณอยู่ก๊วนนั้น" ต้อง query `group_members`
ซึ่งเป็นตารางเดียวกับที่ policy กำลังบังคับ → **infinite recursion**

วิธีแก้คือแบบเดียวกับที่โปรเจกต์ใช้อยู่แล้ว: helper `is_group_member(uuid)`
เป็น `security definer` ซึ่งข้าม RLS ภายในตัวมันเอง policy จึงเรียกได้โดยไม่วน
เขียนไว้ตรงนี้เพราะเป็นกับดักที่เจอตอนรันเท่านั้น ไม่เจอตอนอ่านโค้ด

**3. แจ้งเตือนตอนตั้งนัดจากก๊วน ต้องหนึ่งแถวต่อคน และไม่รวมผู้ตั้ง**

บทเรียนจาก LSN-0024 — วนแล้วยิงทีละแถวได้แจ้งเตือนซ้ำ ใช้
`where user_id <> auth.uid()` บน `group_members` ซึ่งมี PK เป็น
`(group_id, user_id)` อยู่แล้ว จึงไม่มีทางซ้ำโดยโครงสร้าง

### Task 1 — migration: ตาราง สิทธิ์ และคอลัมน์บน sessions

`supabase/migrations/20260918000100_groups.sql`

- `groups`, `group_members` ตามสคีมาในตั๋ว
- `groups_single_owner` — partial unique index บังคับ owner คนเดียวที่ระดับฐานข้อมูล
- `generate_group_code()` + trigger `groups_assign_code()`
- `alter table sessions add column group_id uuid references groups(id) on delete set null`
  **nullable ไม่มี default ไม่มี trigger** — นี่คือสิ่งเดียวที่ทำให้ AC ข้อ
  "ตั้งนัดโดยไม่มีก๊วนยังทำได้เหมือนเดิม" เป็นจริงโดยโครงสร้าง ไม่ใช่โดยความระวัง
- `grant all privileges on public.groups, public.group_members to service_role`
  **บทเรียนจาก LSN-0024** — `grant ... on all tables` ใน `20260901000300_rls.sql`
  เป็นคำสั่งครั้งเดียว ตารางที่สร้างทีหลังไม่ได้สิทธิ์ตามไปด้วย

### Task 2 — RLS + helper

- `is_group_member(p_group_id uuid)` และ `is_group_owner(p_group_id uuid)`
  ทั้งคู่ `security definer` `stable` `set search_path = public, pg_temp`
- `groups` อ่านได้ถ้าเป็นสมาชิก หรือเป็นแอดมิน — **และอ่านด้วย `public_code` ได้
  โดยคนนอก** เพราะหน้าเชิญต้องแสดงชื่อก๊วนก่อนกดเข้าร่วม ทำผ่าน RPC
  `group_invite_public(text)` ที่คืนเฉพาะ ชื่อ · กีฬา · จำนวนสมาชิก **ไม่คืนรายชื่อ**
  (RLS เป็น row-level ไม่ใช่ column-level — บทเรียนจาก LSN-0023)
- `group_members` อ่านได้เฉพาะสมาชิกของก๊วนนั้น เขียนผ่าน RPC เท่านั้น

### Task 3 — RPC

| ฟังก์ชัน | กติกา |
| --- | --- |
| `create_group(name, sport_id, home_district)` | ผู้สร้างเป็น `owner` อัตโนมัติในทรานแซกชันเดียวกัน |
| `join_group(p_code text)` | `on conflict (group_id, user_id) do nothing` → เข้าซ้ำไม่สร้างแถวซ้ำ · ก๊วนที่ archive แล้วเข้าไม่ได้ |
| `leave_group(p_group_id)` | **owner ออกไม่ได้** คืน `reason: 'owner_cannot_leave'` |
| `archive_group(p_group_id)` | เฉพาะ owner · set `archived_at` ไม่ลบแถว |

ทุกตัวเรียก `public.app_log(...)` — **ชื่อ helper คือ `app_log` ไม่ใช่ตาราง
`audit_logs`** (เคย grep ผิดมาแล้วตอน review LSN-0024)

### Task 4 — ผูกนัดกับก๊วน + แจ้งเตือน

- `createSessionAction` รับ `groupId` เพิ่ม (optional) และ validate ว่าผู้ตั้งเป็นสมาชิก
- RPC `notify_group_new_session(p_session_id)` ยิงแจ้งเตือนหนึ่งแถวต่อสมาชิก
  ยกเว้นผู้ตั้ง
- ถ้าไม่ส่ง `groupId` มา **เส้นทางเดิมต้องไม่เปลี่ยนแม้แต่บรรทัดเดียว**

### Task 5 — pgTAP `supabase/tests/groups.test.sql`

ปักทุกข้อใน Acceptance Criteria โดยเฉพาะสามข้อนี้ที่พังเงียบได้

- owner คนที่สองถูกปฏิเสธที่ระดับฐานข้อมูล → `throws_ok` พร้อม **ชื่อ index**
  ไม่ใช่แค่ SQLSTATE (บทเรียนจาก LSN-0024 ที่เทสผ่านทั้งที่ constraint ถูกถอด)
- **ตั้งนัดโดยไม่มีก๊วนยังได้เหมือนเดิม** — เทสข้อนี้คือด่านที่กันไม่ให้ตั๋วนี้
  ทำลายทางเข้าหลักของโปรดักต์
- คนนอกอ่าน `group_members` ไม่ได้ — รันในฐานะ `authenticated` ที่ไม่ใช่สมาชิก

### Task 6 — seed

ก๊วนตัวอย่างหนึ่งก๊วนที่มี owner + สมาชิก 2 คน และผูกกับนัด `BOOKED1`
เพื่อให้เห็นความสัมพันธ์จริงบนหน้าจอ

### Task 7 — ฝั่ง TypeScript

`src/types/database.ts` · `src/lib/actions/group.ts` · `src/lib/queries.ts`
โดเมนไม่มีการคำนวณที่ต้องแยกไฟล์ จึงไม่มี `src/lib/domain/*` ใหม่

### Task 8 — UI

- `/app/groups` รายการก๊วนของฉัน
- `/app/groups/[id]` หน้าก๊วน: สมาชิก · ลิงก์เชิญ · ปุ่มตั้งนัด · ออก/archive
- `/g/[code]` หน้ารับเชิญ เปิดได้โดยไม่ต้องล็อกอิน แสดงชื่อก๊วนและกีฬา
- เพิ่มเมนู **"ก๊วนของฉัน"** ใน nav — คำนี้ว่างแล้วหลัง LSN-0027

### Task 9 — ตรวจของจริง

`db:reset` · `test:db` · `verify` · `build` แล้วตรวจเบราว์เซอร์ 375×812
สามมุมมอง: เจ้าของก๊วน · สมาชิก · คนนอกที่กดลิงก์เชิญ

### ความเสี่ยงที่รู้ตัวอยู่

| ความเสี่ยง | กัน |
| --- | --- |
| **ทำลายการตั้งนัดแบบไม่มีก๊วน** | `group_id` nullable ไม่มี default ไม่มี trigger + เทสปักไว้ |
| RLS วนลูปบน `group_members` | helper `security definer` |
| คนนอกเห็นรายชื่อสมาชิกผ่านหน้าเชิญ | RPC คืนเฉพาะ 3 ฟิลด์ ไม่คืนรายชื่อ |
| `service_role` ไม่มีสิทธิ์บนตารางใหม่ | grant ตรง ๆ ใน migration |
| แจ้งเตือนซ้ำ | PK `(group_id, user_id)` + ไม่รวมผู้ตั้ง |

---

## Blocking notes

- ~~**LSN-0027 ต้อง merge ก่อนตั๋วนี้เริ่ม** ยังไม่ได้สร้างตั๋วนั้น~~
  **หลุดแล้ว** — LSN-0027 สร้าง ปิด และ merge เข้า main ครบเมื่อ 17 ก.ย. 2569
  คำว่า "ก๊วน" ว่างทั้งแอปแล้ว ตรวจจาก `src/` และ `prosrc` ได้ 0 จุด

  เหตุผล: วัดแล้วคำว่า "ก๊วน" อยู่ใน **40 ไฟล์** — `src/i18n/th.ts` 27 ครั้ง และ
  กระจายในอีก 39 ไฟล์ราว 100 ครั้ง ข้อความไทยในโปรเจกต์นี้ไม่ได้รวมศูนย์ที่ i18n

  ที่สำคัญกว่าจำนวนคือ **เมนูใน nav ชื่อ "ก๊วนของฉัน" ซึ่งวันนี้แปลว่า *นัดของฉัน***
  ทันทีที่ตั๋วนี้เพิ่มก๊วนที่เป็นกลุ่มคน เมนูนั้นจะมีสองความหมายบนหน้าจอเดียวกัน
  ถ้าเปลี่ยนคำก่อน คำว่า "ก๊วน" จะว่างพอดีให้ของใหม่ใช้ และไม่มีช่วงที่ผู้ใช้เห็น
  สองความหมายเลยแม้แต่วันเดียว

- ตั๋วนี้เป็นใบแรกของทิศทางทัวร์นาเมนต์ตาม `.codex/specs/tournaments.md`
  **เลขในแผนเดิมใช้ไม่ได้แล้ว** — LSN-0028 ถูกใช้ไปกับตั๋ว **Badge** (17 ก.ย. 2569)
  ใบที่เหลือจึงเลื่อนเป็น LSN-0029 (ทัวร์นาเมนต์) · LSN-0030 (ผลแมตช์) ·
  LSN-0031 (คะแนนฝีมือ) · LSN-0032 (เพื่อนและความประทับใจ) — ยังไม่เปิดตั๋ว
- ไม่ทับกับ LSN-0024 และ LSN-0025 ทำคู่ขนานได้
