# 🟢 LSN-0027 — เปลี่ยนคำ: ก๊วน → นัด

**Type:** Feature · **Phase:** 10 · **Status:** done
**Spec:** `.codex/specs/tournaments.md` หัวข้อ Decisions ข้อ 2
**Depends on:** ไม่มี — **แต่ตั๋วนี้ต้อง merge ก่อน LSN-0026 เริ่ม**

## Goal

คำว่า **"ก๊วน"** ในแอปทุกวันนี้หมายถึง *การนัดเล่นหนึ่งครั้ง* เปลี่ยนเป็น **"นัด"**
เพื่อปล่อยคำว่า "ก๊วน" ให้ว่างสำหรับ *กลุ่มคน* ที่ LSN-0026 จะเพิ่มเข้ามา

## Context

`sessions` หนึ่งแถวคือการนัดเล่นหนึ่งครั้ง แต่ UI เรียกมันว่า "ก๊วน" ส่วน flow
ทัวร์นาเมนต์ใช้คำว่าก๊วนหมายถึงกลุ่มคนที่เล่นด้วยกันประจำ

**ตัวที่บังคับว่าต้องทำก่อน ไม่ใช่ทำทีหลัง:** เมนูใน nav ชื่อ **"ก๊วนของฉัน"**
ซึ่งวันนี้แปลว่า *นัดของฉัน* ทันทีที่ LSN-0026 เพิ่มก๊วนที่เป็นกลุ่มคน ป้ายเดียวนั้น
จะมีสองความหมายบนหน้าจอเดียวกัน ถ้าเปลี่ยนคำก่อน จะไม่มีช่วงที่ผู้ใช้เห็นสอง
ความหมายเลยแม้แต่วันเดียว

ภาษาไทยใช้ "ก๊วน" ได้ทั้งสองความหมายอยู่แล้ว การเลือกให้ก๊วน = กลุ่มคน คือการเลือก
ความหมายที่ตรงกับที่คนแบดใช้พูดถึงชุมชนตัวเอง ("อยู่ก๊วนนี้มาสองปีแล้ว")
ส่วน "นัด" ก็เป็นคำที่ใช้กันอยู่แล้ว ("นัดตีแบดวันพุธ")

## ขอบเขตจริง — วัดใหม่ตอนเริ่มทำ

> ตัวเลขชุดแรกวัดไว้ตอน **LSN-0024 ยังไม่ merge** พอ merge แล้วขอบเขตโตขึ้น
> และมี **ฟังก์ชันที่ห้าและหก** ที่ตั๋วฉบับแรกไม่รู้จัก เก็บตัวเลขเดิมไว้ในวงเล็บ
> เพื่อให้เห็นว่าอะไรเปลี่ยน ไม่ใช่เขียนทับให้ดูเหมือนวัดถูกตั้งแต่แรก

| ที่ | จำนวนจริง | ที่เขียนไว้ตอนแรก |
| --- | --- | --- |
| `src/i18n/th.ts` | 31 ครั้ง | (27) |
| `src/` ทั้งหมด | **133 ครั้ง ใน 42 ไฟล์** | (~100 ใน 39 ไฟล์) |
| ข้อความที่ผู้ใช้เห็นฝั่ง SQL | **4 ฟังก์ชัน** | (3) |
| คำเก่าในคอมเมนต์ของฟังก์ชัน | **อีก 2 ฟังก์ชัน** | (ไม่ได้นับ) |
| `comment on` ที่เก็บใน catalog | **2 จุด** | (ไม่ได้นับ) |
| `supabase/seed.sql` | 6 ครั้ง | (4) |
| ชื่อเทสใน pgTAP | 25 ครั้ง ใน 6 ไฟล์ | (~7) |

**ฟังก์ชันที่ห้า** `send_session_charges` มาจาก LSN-0024 มีข้อความแจ้งเตือน
*"ก๊วนนี้มีรายการเพิ่มเติม…"* ซึ่งเป็นข้อความที่ผู้ใช้เห็นเต็ม ๆ

**ฟังก์ชันที่หกและเจ็ด** `confirm_court_manually` (LSN-0025) กับ
`create_session_charge` (LSN-0024) มีคำเก่าใน **คอมเมนต์** ไม่ใช่ข้อความที่ผู้ใช้เห็น
สร้างใหม่ด้วยเพื่อไม่ให้เหลือคำปนกันในโค้ด

## ส่วนที่พลาดง่ายที่สุด: ข้อความแจ้งเตือนอยู่ใน SQL

การแก้แค่ `src/` จะทำให้แจ้งเตือนยังใช้คำเก่า ผู้ใช้เห็นสองคำในแอปเดียวต่อไป
มี **5 จุด** อยู่ใน **3 ฟังก์ชันที่ยังทำงานจริง**:

| ฟังก์ชัน | นิยามล่าสุดอยู่ที่ | ข้อความ |
| --- | --- | --- |
| `promote_waitlist` | `20260901000200_functions.sql:1024` | "มีที่ว่างในก๊วนที่คุณรออยู่…" |
| `mark_no_shows` | `20260909000100_check_in.sql:136` | "ก๊วนจบแล้วแต่ไม่มีการเช็คอิน…" |
| `void_pay_later_on_session_end` | **`20260911000100_guest_participants.sql:432`** | "ก๊วนถูกยกเลิกและระบบไม่ได้จองสนามให้…" |

### ⚠️ กับดักของ `void_pay_later_on_session_end`

ฟังก์ชันนี้ถูก `create or replace` **สามครั้ง** ในสาม migration:
`20260905000100_pay_later_credit.sql:514` → `20260910000100_settlement.sql:240` →
`20260911000100_guest_participants.sql:432`

**ตัวที่มีผลจริงคือตัวสุดท้าย** ตรวจในฐานข้อมูลแล้วยืนยันว่าเป็นตัวของ LSN-0022
เพราะมี guard `if v_row.user_id is null then continue` อยู่ในลูป

**ถ้าใครคัดลอกจากไฟล์แรกหรือไฟล์กลางมาแก้คำ จะย้อน guard นั้นทิ้งโดยเงียบ ๆ**
guard นั้นคือสิ่งที่แก้ blocker ของ LSN-0022 ซึ่งทำให้**ก๊วนที่มีผู้เล่นรับเชิญค้างจ่าย
ยกเลิกไม่ได้เลย** เพราะ `notify_user(null, …)` ชน NOT NULL ของ `notifications.user_id`
แล้ว UPDATE ล้มทั้งคำสั่ง

→ ต้องคัดลอกจาก `20260911000100_guest_participants.sql` เท่านั้น และมีเทสยืนยัน

### ห้ามแก้ migration เก่า

การแก้ข้อความใน migration ที่ apply ไปแล้ว **ไม่เปลี่ยนฟังก์ชันที่ทำงานอยู่จริง**
`npm run db:reset` ในเครื่องจะเห็นคำใหม่ แต่ฐานข้อมูลบนคลาวด์ยังใช้คำเก่า
แล้วเครื่องกับของจริงจะไม่ตรงกันโดยไม่มีใครรู้

→ ต้องเพิ่ม **migration ใหม่** ที่ `create or replace` ทั้งสามฟังก์ชัน

## Decisions — ตัดสินแล้ว

### 1. คำที่ใช้แทน

| เดิม | ใหม่ |
| --- | --- |
| ก๊วน (การนัดเล่นครั้งหนึ่ง) | **นัด** |
| ก๊วนของฉัน | **นัดของฉัน** |
| หาก๊วน | **หานัด** |
| สร้างก๊วน · จัดการก๊วน | **สร้างนัด · จัดการนัด** |
| ใบเสร็จก๊วน | **ใบเสร็จนัด** |

### 2. คำว่า "ผู้จัดก๊วน" ต้องหายไป ไม่ใช่แค่เปลี่ยนคำต่อท้าย

ใช้ **"ผู้จัด"** เปล่า ๆ ตรงที่บริบทชัด และ **"ผู้จัดนัด"** ตรงที่ต้องขยาย

เหตุผล: LSN-0026 จะเพิ่ม `group_members.role = 'owner'` ซึ่งคือ *เจ้าของก๊วน*
ถ้าปล่อยคำว่า "ผู้จัดก๊วน" ไว้ มันจะกลายเป็นชื่อของอีกบทบาทหนึ่งทันที
รวมถึง `display_name` ใน seed ที่เขียนว่า "ก้อง ผู้จัดก๊วน"

### 3. ชื่อนัดตัวอย่างใน seed → เปลี่ยนด้วย

`'ก๊วนแบดเย็นวันพุธ'` → `'แบดเย็นวันพุธ'` เพราะในโลกจริงผู้ใช้ตั้งชื่อนัด
ไม่ได้เอาคำว่านัดมานำหน้า และ seed คือสิ่งที่คนในทีมเห็นบ่อยที่สุด

### 4. ไม่ทำ automated guard กันคำเก่ากลับมา

เคยคิดจะเพิ่มเทสที่ห้ามคำว่า "ก๊วน" ใน `src/` แต่**ทำไม่ได้** เพราะ LSN-0026
จะใช้คำนั้นอย่างถูกต้องสำหรับกลุ่มคน เทสแบบนั้นจะต้องถูกถอดทันทีในตั๋วถัดไป
ซึ่งแย่กว่าไม่มีเลย → อาศัย review ของตั๋วนี้ และบันทึกไว้ตรงนี้ว่าเป็นการตัดสินใจ
ไม่ใช่การลืม

### 5. AC ของ LSN-0023 จะกลายเป็นเท็จ และต้องบันทึกไว้

LSN-0023 (done แล้ว) มี acceptance criteria ข้อหนึ่งเขียนว่า

> คำที่ใช้: **"ใบเสร็จก๊วน"** · "ค้าง" · "จ่ายแล้ว" — ห้ามใช้คำว่า invoice

ตั๋วนี้เปลี่ยนคำนั้นเป็น "ใบเสร็จนัด" → ต้องไปเขียนหมายเหตุในตั๋ว LSN-0023
ว่าคำถูกเปลี่ยนโดยตั๋วนี้ **ไม่ใช่แก้ AC ของตั๋วที่ปิดแล้วให้ดูเหมือนไม่เคยมีอะไรเกิดขึ้น**
ส่วน "ห้ามใช้คำว่า invoice" ยังมีผลอยู่

## Scope

- แก้ข้อความใน `src/i18n/th.ts` และอีก 39 ไฟล์ใน `src/`
- migration ใหม่ที่ `create or replace` 3 ฟังก์ชัน เพื่อแก้ข้อความแจ้งเตือน
- แก้ `supabase/seed.sql`
- แก้ comment และชื่อเทสเพื่อความสม่ำเสมอ
- เขียนหมายเหตุในตั๋ว LSN-0023

## Non-goals

- ไม่เปลี่ยนชื่อตาราง คอลัมน์ ฟังก์ชัน หรือ route — `sessions`, `/s/[code]`
  และชื่อฟังก์ชันทั้งหมดอยู่เหมือนเดิม ตั๋วนี้เปลี่ยน **ข้อความที่ผู้ใช้เห็น** เท่านั้น
- ไม่เพิ่มตาราง `groups` — เป็นงานของ LSN-0026
- ไม่แตะโครงสร้างหรือพฤติกรรมใด ๆ

## Acceptance Criteria

- [x] ไม่มีคำว่า "ก๊วน" เหลือใน `src/` ในความหมายของการนัดเล่น — ตรวจด้วย
      `grep -rn "ก๊วน" src` ต้องได้ผลว่าง
- [x] ไม่มีคำว่า "ก๊วน" เหลือในข้อความที่ผู้ใช้เห็นฝั่ง SQL — ตรวจด้วยการอ่าน
      `prosrc` ของทั้งสามฟังก์ชันจากฐานข้อมูลจริง ไม่ใช่อ่านไฟล์
- [x] migration ใหม่ `create or replace` `void_pay_later_on_session_end` จากตัวของ
      `20260911000100_guest_participants.sql` และ **guard ผู้เล่นรับเชิญยังอยู่** —
      ปักด้วยเทส: ก๊วนที่มีผู้เล่นรับเชิญค้างจ่ายต้องยกเลิกได้
- [x] `promote_waitlist` และ `mark_no_shows` ยังทำงานเหมือนเดิมทุกอย่าง — pgTAP
      ของ LSN-0019/0020 ยังเขียวโดยไม่ต้องแก้เทส
- [x] ไม่มีคำว่า "ผู้จัดก๊วน" เหลือที่ไหนเลย รวมใน seed
- [x] `npm run test:db` ผ่านทุกไฟล์ · ไม่มีไฟล์เทสไหนถูกแก้เพื่อให้ผ่าน
- [x] พฤติกรรมไม่เปลี่ยนเลย — ไม่มี migration ที่แตะสคีมา

## Validation

- `npm run lint`, `npx tsc --noEmit`, `npm test` ผ่าน
- `npm run test:db` ผ่าน
- `npm run build` ผ่าน
- ตรวจบนเบราว์เซอร์ 375×748 ทุกหน้าที่มีคำนี้: หน้าแรก · หานัด · นัดของฉัน ·
  จัดการนัด · หน้านัดสาธารณะ · เข้าร่วม · คิวสำรอง · ใบเสร็จ · โปรไฟล์ · ฝั่งสนาม · แอดมิน
- ตรวจข้อความแจ้งเตือนจริงหนึ่งครั้ง โดยยกเลิกนัดที่มีคนค้างจ่าย แล้วอ่านแถวใน
  `notifications` ว่าใช้คำใหม่

## Review Notes

**วันที่:** 17 ก.ย. 2569 · **สาขา:** `feature/LSN-0027`

### ด่านที่รัน

| ด่าน | ผล |
| --- | --- |
| `npm run db:reset` | ผ่าน — migration 13 ไฟล์ apply ได้จากศูนย์ |
| `npm run test:db` | ผ่าน — 110 เทส 10 ไฟล์ |
| `npm run verify` | ผ่าน — lint, `tsc --noEmit`, vitest 143 เทส |
| `npm run build` | ผ่าน |
| เบราว์เซอร์ 375×748 | ผ่าน 16 หน้า ทั้งสาธารณะและหลังล็อกอิน |

### ตรวจ AC จากของจริง ไม่ใช่จากไฟล์

| เกณฑ์ | วิธีตรวจ | ผล |
| --- | --- | --- |
| ไม่มีคำเก่าใน `src/` | `grep -ro` | **0** |
| ไม่มี "ผู้จัดก๊วน" ที่ไหนเลย | `grep -ro src supabase` | **0** |
| ไม่มีคำเก่าในข้อความฝั่ง SQL | อ่าน `prosrc` จากฐานข้อมูล | **0 ฟังก์ชัน** |
| guard ผู้เล่นรับเชิญยังอยู่ | ยกเลิกนัดที่มีผู้เล่นรับเชิญค้างจ่ายจริง | สำเร็จ |
| pgTAP ของ LSN-0019/0020 ไม่ต้องแก้ | `git diff` ไฟล์เทส | แก้เฉพาะคำบรรยาย ไม่แตะ assertion |

ข้อ "ไม่มีคำเก่าในข้อความฝั่ง SQL" **ต้องตรวจจาก `prosrc` เท่านั้น** และมันจับได้จริง
สองรอบ — รอบแรกเจอฟังก์ชันที่ห้าที่ตั๋วไม่รู้จัก รอบสองเจออีกสองตัวที่มีคำเก่า
ในคอมเมนต์ ถ้าตรวจจากไฟล์อย่างเดียวจะผ่านทั้งที่ยังไม่เสร็จ

### ขอบเขตโตกว่าที่ตั๋วเขียน

ตั๋ววัดไว้ตอน LSN-0024 ยังไม่ merge อัปเดตตารางในตั๋วแล้วโดยเก็บตัวเลขเดิม
ไว้ในวงเล็บ ฟังก์ชันที่ตั๋วไม่รู้จักมีสามตัว — `send_session_charges`
(ข้อความที่ผู้ใช้เห็น) · `confirm_court_manually` และ `create_session_charge`
(คำเก่าในคอมเมนต์)

### สามจุดที่แทนคำตรง ๆ แล้วความหมายเพี้ยน

การแทนคำอัตโนมัติผลิตภาษาไทยที่ผิดความหมายในสามจุด ต้องอ่านผลลัพธ์ทุกบรรทัด
ไม่ใช่เชื่อว่าแทนคำแล้วจบ

1. **`รวมก๊วน` คือ H1 หน้าแรกและ tagline ของแบรนด์** แปลว่ารวม *คน*
   `รวมนัด` อ่านเป็นการรวมการนัดหลายอัน → ใช้ **`รวมคน`** ซึ่งพูดสิ่งที่ตั้งใจ
   จะพูดจริง และยังถูกต้องต่อไปหลัง LSN-0026 ที่ก๊วนจะกลับมาหมายถึงกลุ่มคน
2. `ดูรายชื่อได้เฉพาะคนในก๊วน` → `เฉพาะคนที่ร่วมนัดนี้` เพราะ "คนในนัด" ไม่เป็นไทย
3. ชื่อนัดใน seed: ได้ `นัดแบดเย็นวันพุธ` ซึ่งผิด Decision 3 → `แบดเย็นวันพุธ`
   **เจอจากการดูหน้าจอจริง ไม่ใช่จากการอ่าน diff**

### กับดักของตั๋วนี้ ปิดด้วยโครงสร้าง ไม่ใช่ด้วยความระมัดระวัง

`void_pay_later_on_session_end` ถูก `create or replace` สามครั้ง และตัวที่มีผล
คือตัวที่มี guard ผู้เล่นรับเชิญจาก LSN-0022

migration นี้ generate โดยไล่ทุกไฟล์ตามลำดับ timestamp หยิบนิยามท้ายสุด แล้ว
**assert ว่าเนื้อที่หยิบมาตรงกับ `prosrc` ที่ทำงานอยู่จริงในฐานข้อมูล** ทุกตัว
(เทียบแบบ normalise คำ เพื่อให้ด่านไม่เป็นวงกลมเมื่อ migration รันไปแล้ว)
บวก assert แยกอีกชั้นว่า guard ยังอยู่ — การคัดลอกผิดตัวจึงเป็นไปไม่ได้
ไม่ใช่แค่ระวังแล้วหวังว่าถูก

### เรื่องที่บันทึกไว้ให้ชัด

- **แก้ไฟล์เทส 6 ไฟล์จริง** แต่แก้เฉพาะคำในคำบรรยายและคอมเมนต์
  **ไม่มี assertion ค่าคาดหวัง หรือ plan() ไหนถูกแตะ** — ไม่ใช่การแก้เทสให้ผ่าน
- เขียนหมายเหตุใน LSN-0023 ตาม Decision 5 แล้ว ไม่ได้แก้ AC ของตั๋วที่ปิดไปแล้ว
- ไม่มี automated guard กันคำเก่ากลับมา ตาม Decision 4 ซึ่งเป็นการตัดสินใจ
  ไม่ใช่การลืม — LSN-0026 จะใช้คำนั้นอย่างถูกต้องสำหรับกลุ่มคน

### ผลการ review (17 ก.ย. 2569) — `PASS`

ใช้ `scrutinize` เพราะตั๋วสร้างฟังก์ชัน SQL ใหม่หกตัว ซึ่งเป็นความเสี่ยงจริง
ตามที่ตั๋วเตือนไว้เอง ไม่ใช้ `fullstack-guardian` เพราะไม่มีสกิลนี้ในโปรเจกต์นี้

**ตรวจอิสระ ไม่เชื่อ assert ที่เขียนไว้ตอน generate** — ดึงฟังก์ชันทั้งหกจาก
migration ใหม่มาเทียบกับนิยามท้ายสุดในไฟล์เดิมทีละตัวอักษร ผลคือ
**ทั้งหกตัวเหมือนต้นฉบับทุกตัวอักษร ยกเว้นคำที่ตั้งใจเปลี่ยนจุดเดียว**
ไม่มีบรรทัดตรรกะไหนถูกแตะ

**ช่องที่ `prosrc` ตรวจไม่เจอ** — `comment on` เก็บใน `pg_description`
ไม่ใช่ `prosrc` การเช็ค `prosrc like '%ก๊วน%'` จึงไม่ครอบข้อนี้ ตรวจแยกแล้ว
`pg_description` สะอาด แปลว่า `comment on` สองจุดในไฟล์ทำงานจริง

**ไม่มีโค้ดที่เทียบกับสตริงไทย** — ถ้ามี การเปลี่ยนคำข้างเดียวจะทำให้เงื่อนไข
ไม่ตรงกันเงียบ ๆ ไล่แล้วไม่มีสักจุด

**NEEDS FIX หนึ่งข้อ แก้แล้ว** — `README.md:100` มีขั้นตอนบอกให้กดเมนูชื่อ
**"จัดการก๊วน"** ซึ่งตั๋วนี้เพิ่งเปลี่ยนเป็น "จัดการนัด" คำสั่งนั้นจึงพาไปหาเมนู
ที่ไม่มีอยู่แล้ว อยู่นอก scope ที่ตั๋วเขียนไว้ แต่เป็นของที่**ตั๋วนี้ทำให้ผิดเอง**

**ที่เหลือคำเก่าไว้โดยตั้งใจ** — `docs/` ทั้งโฟลเดอร์ โดยเฉพาะ
`badges/badge-definitions.json` ที่เขียนว่า *"จบแมตช์กับก๊วนที่ไม่เคยแข่งด้วยกัน"*
ซึ่งใช้คำในความหมาย **กลุ่มคน** อันเป็นความหมายที่ LSN-0026 จะทำให้ถูกต้อง
การไปแก้จะเป็นการทำให้ผิด ส่วน `docs/product-concept.md` ก็เช่นกัน

### ยังไม่ทำ

- ยังไม่ merge — **ตั๋วนี้ต้อง merge ก่อน LSN-0026 เริ่ม**

---

## Done Notes

**วันที่:** 17 ก.ย. 2569 · **สาขา:** `feature/LSN-0027`

### ด่านที่รัน

| ด่าน | ผล |
| --- | --- |
| `npm run db:reset` | ผ่าน — migration 13 ไฟล์ apply ได้จากศูนย์ |
| `npm run test:db` | ผ่าน — 110 เทส 10 ไฟล์ |
| `npm run verify` | ผ่าน exit 0 — lint, `tsc --noEmit`, vitest 143 เทส |
| `npm run build` | ผ่าน exit 0 |
| เบราว์เซอร์ 375×748 | ผ่าน 16 หน้า |
| ตรวจ `prosrc` + `pg_description` | 0 จุดที่ยังมีคำเก่า |

### ด่านที่ใช้และไม่ได้ใช้

- **ใช้ `scrutinize`** เพราะตั๋วสร้างฟังก์ชัน SQL ใหม่หกตัว และมันจับได้หนึ่งข้อ
  (ขั้นตอนใน README ที่ตั๋วนี้ทำให้ผิด) พร้อมยืนยันว่าทั้งหกฟังก์ชันเหมือน
  ต้นฉบับทุกตัวอักษรยกเว้นคำ
- **ไม่ใช้ `post-mortem`** เพราะตั๋วนี้ **ไม่ใช่ bugfix** ไม่มีบั๊กที่ต้องเก็บ
  root cause ไว้ ส่วนข้อที่ review เจอเป็นเอกสารคลาดเคลื่อนหนึ่งบรรทัด
  ซึ่งบันทึกไว้ใน Review Notes พอแล้ว
- **ไม่ใช้ `fullstack-guardian`** — ไม่มีสกิลนี้ในโปรเจกต์ longsanam

### สิ่งที่ตั๋วนี้ปลดล็อก

คำว่า **"ก๊วน"** ว่างแล้ว **LSN-0026 เริ่มได้** และจะไม่มีช่วงไหนที่ผู้ใช้
เห็นคำเดียวสองความหมายบนหน้าจอเดียวกัน ซึ่งเป็นเหตุผลที่ตั๋วนี้ต้องมาก่อน

### ยังไม่ทำ

- ยังไม่ merge — **ต้อง merge ก่อน LSN-0026 เริ่ม**
- migration `20260917000100_rename_session_wording.sql` ยังไม่เคยรันบน
  Supabase cloud ต้องรันตอน deploy ไม่งั้นแจ้งเตือนของจริงจะยังใช้คำเก่า
  ทั้งที่หน้าจอใช้คำใหม่แล้ว

---

## Blocking notes

- **ตั๋วนี้ต้อง merge ก่อน LSN-0026 เริ่ม**
- ไม่ทับกับ LSN-0024 และ LSN-0025 แต่ถ้าสองใบนั้นยังไม่ merge จะเกิด conflict
  ที่ `src/i18n/th.ts` เกือบแน่ — ตาม memory ของโปรเจกต์ให้ merge `origin/main`
  เข้าสาขาที่เปิดอยู่ก่อน แล้วทำตั๋วนี้ทีหลังสุด
